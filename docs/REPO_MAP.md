# Repository map

Snapshot at commit `bb50904` (baseline import, 2026-07-17). Purposes below are derived from
reading the files, not from filenames. Ignored paths (`venv/`, `__pycache__/`, `.claude/`,
`.remember/`) are excluded.

## Canonical pipeline — `code/` (1,932 LOC total)

| File | LOC | Modified | Purpose |
|---|---|---|---|
| `mbo.py` | 632 | 07-15 02:51 | The engine. Synthetic `Task` classes (raw-`y` oracles, seed-0 fixed data), three surrogates (`train_ensemble`, `botorchgp`, `svgp`), three optimizers (`grad_opt`, `perturb_opt`, CMA), LCB closures, conformal repair, `init_candidates`/`eval_designs`, `run_calibration`, O2O loop, legacy baselines (`coms`, CEM-style "CbAS", `sparse_gp`). |
| `figures.py` | 417 | 07-15 15:40 | Generates `paper/figures_v2/*` from the two primary result files. Okabe-Ito palette. |
| `run05.py` | 234 | 07-15 11:05 | The paper's analysis layer. Hand-rolled η² (no statsmodels), SEI/OEI, GATE-1 retention, rank/CD/TOST → `results/05_findings.json`. |
| `run_all.py` | 189 | 07-13 20:08 | Experiment driver. `--exp`, `--seeds`, `--jobs`, `--db`, `--db-subsample`. Per-cell exception handling. Writes `results_camera.json` / `results_db.json`. |
| `stats.py` | 164 | 07-13 06:20 | Wilcoxon+Holm, Friedman, bootstrap mean-ranks (resamples **tasks**, not seeds). |
| `analysis.py` | 142 | 07-13 06:18 | `task_norm` per-task min-max normalization — the analysis-time normalizer (matches any key containing `':'`, so it spans 11 cells, not 9). |
| `db_tasks.py` | 109 | 07-13 16:48 | Design-Bench adapters. One-hot vertex encoding for sequence tasks; RF-oracle substitution. |
| `quicklook.py` | 85 | 07-14 19:38 | Ad-hoc result inspector. |
| `run_beta0.py` | 84 | 07-15 04:30 | β=0 control arm. |
| `tables.py` | 74 | 07-11 02:10 | LaTeX table generator → `paper/tables_v2/*`. |
| `gradtune.py` | 71 | 07-12 05:19 | Gradient trust-region / normalization sweep → `results_gradtune.json`. |
| `run_gpcov.py` | 68 | 07-15 11:07 | Cross-proposal coverage. Uses **sklearn's** GP, not the grid's `botorchgp`; takes ensemble proposals from gradient and GP proposals from perturbation. |
| `run_subsample.py` | 63 | 07-15 11:07 | Data-subsample control (ensemble given the GP's subsample). |

## Cloud / orchestration — `cloud/`

`setup.sh` (83) three conda envs + miniconda bootstrap · `fix_designbench.sh` (61) eight patches to make
Design-Bench import (HF-mirror data, numpy/sklearn pins) · `Dockerfile` (14) · `local_queue.sh` (31) ·
`queue/00_smoke.sh` … `queue/05_analyze.sh` + `run_queue.sh` — a numbered dependency-ordered run queue
with a README.

## Paper — `paper/`

- **`aaai27/main.tex` (298 LOC, 07-15 16:31)** — the live AAAI-27 anonymous submission. `main.pdf` compiled.
- **`aaai27/supplement.tex` (206 LOC)** — supplement; `supplement.pdf` compiled.
- `aaai27/references.bib`, `aaai2027.sty/.bst`, `AuthorKit27/` (unmodified AAAI kit + Word variants).
- `figures_v2/` — 8 figures × (pdf+png), generated by `figures.py`.
- `tables_v2/` — `grid.tex`, `grid_full.tex`, `grid_rank.tex`, `grid_full_rank.tex`, generated by `tables.py`.
- `proofs.md` (28) — Prop 1 / Prop 2 source. **Numbers disagree with the paper** (q̂ ∈ [2.8, 10.5] vs [1.8, 16]).
- `SKELETON.md` (52) — the manuscript plan. Registered a headline the data refuted; planned an O2O
  contribution that was cut.
- `latex_source/` — the superseded ICML-2026 workshop paper.

## Root

`PREREGISTRATION.md` (66) — the frozen pre-run contract. `README.md` (55) — **stale** (describes the
workshop paper as current, says n=10 seeds). `requirements.txt`. `.gitattributes` (LF for shell).

## Results — `results/`

Primary: **`results_camera.json`** (392 KB, synthetic, 30 seeds, 112/112 cells) and **`results_db.json`**
(141 KB, Design-Bench, 16 seeds, 63/63 grid cells). Derived: `05_findings.json`. Controls: `gpcov.json`,
`results_gradtune.json`, `official_baselines.json` (+ `_raw.tgz`). Superseded: `results.json`,
`results_final.json`, `results_new.json`, `results_revision.json` (pre-grid schema), `*_matched.json`,
`*.preserved.json`, `*.bak`. Full schema/seed analysis in `docs/ARTIFACT_INVENTORY.md`.

## `legacy/`

Frozen history: superseded run scripts, old docs, submission zips. `README.md:16-18` states nothing here
is needed and it must not ship in a supplement. Not audited beyond that.

---

## Orphans and dangling references

**Code no result depends on**
- `gradtune.py` → `results_gradtune.json` exists but **no paper claim cites it**. Commit `cdd5ad8`
  records "trust region closes the ensemble gradient collapse" — a result that contradicts the paper's
  framing and is not reported. This is the most consequential orphan in the repo.
- `run_beta0.py`, `run_subsample.py` → the β=0 and subsample controls are *claimed* in the paper, but
  their values live under `05_findings.json` keys (`beta0`, `subsample_control`) that **`run05.py` never
  writes**. Generator MISSING.
- `quicklook.py` — scratch, unused by the paper.

**Results with no generating code**
- `05_findings.json` keys `bootstrap_ci`, `beta0`, `subsample_control`, `gp_coverage`, `stats_9cell`,
  `rf_robustness` — **written by nothing in the repo**.
- `run_all.py:60` writes `rho_knn`; the field is **absent from both live result files**. The current code
  does not reproduce the current artifacts.

**Figures in the PDF with no script**
- Main **Table 3** (the 77-value Design-Bench grid) has **no counterpart generator** in `tables_v2/`.
- The RF-oracle defense numbers (p=0.93, spreads 0.34 / 0.39) and the TOST bound ±0.48 appear **body-only** —
  no table, no figure, no generator.

**Scripts referencing missing files**
- `SKELETON.md:3,35,44` cites `research/notes/final_report_mbo-decomposition-prior-art-579ba4.md` and
  `research/notes/baseline-numbers-designbench.md` — the R1 novelty verdict and R2 baseline strategy the
  manuscript plan is built on. **Neither exists anywhere on disk.** The paper's positioning and baseline
  strategy rest on two deleted documents.
- `README.md:44` points figures at `legacy/figures/make_figures_v5.py`, which the same README declares
  superseded.

**Stale figures**
- 8 uncited figures in `paper/figures_v2/` and `paper/figures/`. One (`fig4_beta_ablation.pdf`) shows **two**
  tasks where β=0 is competitive, against the paper's "6 of 7, lone exception Ackley."

**Unrun pre-registered work**
- `--seeds 50` for the three crossover-boundary tasks (`PREREGISTRATION.md:14-16`): never run.
- The leave-one-task-out decision rule (`PREREGISTRATION.md:54-59`): never implemented — no `loo`,
  `decision_rule`, or `hit_rate` symbol exists in `code/`.
- The `ScaledAckley` ladder family (`PREREGISTRATION.md:32-36`): instrumented, never run.
