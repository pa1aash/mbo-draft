# Step 14 — Patcher

Run `mbo-gauntlet-r4-adversarial-0f06f1` · branch `gauntlet-r4` · 2026-07-20

**All 42 critic findings applied. Zero skipped, zero escalated.** 15,707 → 20,337 words.

## The three falsified claims, recorded as the query's format demands

Each carries what was claimed, why it fails, and the narrower version that survives.

**1. The three-way ordering.** RANK 1 asserted `surrogate > interaction > optimizer` **"without
exception."** The dialectic critic observed the supporting sweep tested only `η²_surr > η²_opt` —
a different proposition. The test across four corners × three normalizers **inverts in one of
twelve**: on/off under rank normalization gives η²_opt 0.075 against η²_inter 0.049.
**Survives:** the interaction exceeds the optimizer effect in **11 of 12**, by 1.1×–71.3×;
`η²_surr > η²_opt` and surrogate-largest hold in all twelve. The 12-row table is now in the
report. Promotion survives, absolutism does not.

**2. The pairing table.** Claimed the interaction and the 7/7 attenuation "each survive exactly
the challenge the other fails." Two defects: the interaction's normalizer-immune form **is** the
raw-units contrast — one dataset counted twice — and floor-effect immunity was asserted, never
argued, and is plausibly backwards, since a perturbation floor effect would *generate* an
interaction of that shape. **Survives:** two views of one asymmetry, visible in normalized and
raw units, causal reading unestablished. Table cell now reads "existence yes, mechanism no."

**3. The N6 free result.** Used RaM's η²≈0.577 as independent evidence for ground (1). That axis
is **bundled methods**, which the report's own conjunct (b) rejects as not a clean factor.
**Survives:** the bare cross-grid observation, reported as an observation. Ground (1) now rests
explicitly on the UQ taxonomy plus the estimator-family criterion — which the terminal section
already conceded.

Plus the fourth critical: **`UNEXPLORED` labelled** on RANK 6, `UNDER-EXPLAINED` on RANK 8, a new
RANK 8a for frozen cells / LCB paralysis, and a four-category map in the (iii) preamble.

## Conflict resolution

Four logged. The load-bearing one: the width critic's two **bloat** findings against twenty
**additive** findings from depth and instruction. **Coverage and correction won** — as the width
critic itself recommended. Only the two consolidations it named by location were applied; no
broader cuts. **Net length rose**, and the patcher flagged why: the added mass is evidence the
report previously asserted without showing, which is the exact defect three depth findings
convict it of.

## The refusal worth recording

Instruction finding 5 demanded verbatim adversarial-region query strings that **are not in the
artifacts**. The patcher applied that finding's own stated fallback — saying so at the spot —
rather than fabricating plausible ones. The fourteen strings that *are* recorded went in
verbatim. In a report whose subject is citation fidelity, that was the only defensible choice.

## Integrity

102 cited / 102 listed, zero orphans. Twelve new sources ([91]–[102]), each cited in body before
appearing in Sources. Five H2s preserved. Dao, Ghasemipour and Choi keep "venue unverified". None
of the five withdrawn claims re-inflated. Zero authorship strings.
