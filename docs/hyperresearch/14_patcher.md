# 14 · Patcher — change list applied to `11_synthesize.md`

**Not N/A.** The critics (`12_critics.md`) surfaced nine material findings; the synthesis did not hold
unchanged. Acting as the patcher (surgical Read + Edit against the synthesized report, no
regeneration), I applied five hunks that fold in all nine material findings. Notes (D3, D4, DP3, W4,
I3, I4) were acknowledged in-text where cheap (W4 folded into the manuscript-axis line) or left as
standing caveats. No source was added (see `13_gap_fetch.md`) — every fix is a restructuring or a
pointer to an existing repo doc.

## Applied hunks

| # | Location in `11_synthesize.md` | Critic finding(s) resolved | Change |
|---|---|---|---|
| H1 | Accept path, after the three gated items | **I1**, **DP2**, **W4**, plus P1-2/P0-6/P0-7 | Inserted a "Mapped to the four axes the query names" paragraph making **manuscript / experiments / statistics / artifact** legible as structure — carrying the integrity breaks (P0-6, P0-7), the budget-matched arm as separate from X3 (DP2), the ANOVA fix (P1-2), and the novelty phrasing constraint (W4). |
| H2 | Accept path, routing sentence | **W1** | Appended the 7-page over-subscription constraint: Identity C must *cut* (demote a grid table, Props → lemma/remark) even as it *adds* manipulation/interpolation content (`AAAI27_VENUE.md` C.5). |
| H3 | Top-3 risks, after Risk 3 | **W2** | Added the COMs baseline-divergence runner-up risk (2.21 vs official 0.99; P1-6, D7) with its mitigation. |
| H4 | Single-move section, after the PENDING sentence | **DP1**, **DP2**, **I2** | Added the two "keeps this honest" caveats — the run does not fix the statistics route (P1-2) or the surrogate-query-budget imbalance (P1-1) — and named **X4 as the parallel zero-CPU free move** that should not wait on the grid. |
| H5 | Single-move section, corollary | **D1**, **D2**, **W3** | Replaced the C-optimistic one-way corollary with a **three-way branch** (C survives / A-only → MLRC 2026 fallback / η²_surr evaporates → Identity E), elevating E to a live deadline contingency and surfacing the MLRC alternative venue. |

## Findings deliberately NOT patched (with reason)

- **D3 (AAAI-reviews-are-proxy ceiling)** and **D4 (single-corner over-read)** — left as the standing
  caveats they already are in the synthesis's scope note and gate paragraph; patching would add hedge
  bloat without changing the recommendation. The gate paragraph already states the ρ=0.536 flag is
  non-significant at N=7 and that the decisive contrast is unrun.
- **DP3 (X4's own defense burden — the tinyBenchmarks trap)** — a paper-writing detail, not an
  acceptance-strategy finding; it belongs in the X4 section of the manuscript, not the synthesis.
  Recorded here so it is not lost.
- **I3, I4** — the critics affirmed these (scoping disclosure; corner ground-truth use); nothing to fix.

## Net effect

The synthesis's single highest-leverage move is unchanged (it survived all four critics). What changed
is its honesty envelope: the identity recommendation is now a three-way branch gated on the PENDING
run rather than a bet on Identity C, the four query axes are legible, and three previously-omitted
acceptance constraints (page budget, COMs divergence, MLRC fallback) are in-text.
