---
vault_tag: mbo-gp-decomposition-aaai-flaws-519b8a
created: 2026-07-17T03:30:00Z
source: user-prompt
---

Evaluate the AAAI-27 acceptance prospects and scientific flaws of the paper "Decomposing the GP Advantage in Offline MBO" — a controlled surrogate×optimizer factorial decomposition ({deep ensemble, exact GP, sparse variational GP} × {gradient ascent, perturbation, CMA-ES}) over 7 synthetic + 7 Design-Bench tasks, reporting (1) a surrogate main effect that dominates the optimizer main effect (η²=0.37 vs 0.01) with a large interaction (η²=0.17), attributing the cause to the GP's smooth posterior MEAN rather than its calibration; (2) an LCB "premise coverage" diagnostic where the pessimism premise {μ−f ≤ βσ} is shown to be exactly equivalent to LCB validity (Prop 1), with ensemble coverage 0.73 in-distribution, 0.41 on its own gradient-ascent proposals, 0.97 on the GP's proposals, and split-conformal repair (Prop 2) restoring in-distribution but not shifted-proposal coverage; (3) a null result — method choice is highly significant on synthetic tasks (Friedman p=6e-5) but unresolved on Design-Bench (p=0.69, equivalence test underpowered at N=7), i.e. synthetic→real validity collapse.

Evaluate flaws at four levels: (a) the manuscript — claims, framing, positioning, prose, figures, related work; (b) the experiments — identifiability, controls, confounds, optimizer query budgets, baselines; (c) the statistics — validity of every inferential claim (two-way ANOVA on min-max-normalized scores, Friedman/Nemenyi CD, TOST equivalence, bootstrap, Holm); (d) the artifact — reproducibility and what a reviewer running the code would find.

Key questions to research in the literature: What is the prior art on decomposing offline-MBO performance into surrogate vs optimizer factors, and is such a decomposition novel? Is the "GP smooth posterior mean vs jagged ensemble mean / inductive bias" mechanism already established (NTK, spectral bias, Lipschitz smoothness of ensembles vs GPs)? Is coverage-based / conformal diagnosis of LCB pessimism in offline MBO already published (conformal BO, Stanton 2022, Deshpande-Kuleshov 2024, Tibshirani 2019 weighted conformal, CCC/Choi 2026, UNIQ 2026)? What is the state of OFFLINE MODEL SELECTION in offline MBO and offline RL — is a coverage-driven offline surrogate×optimizer selection rule novel and is it a recognized open problem? Is the synthetic→Design-Bench validity collapse / benchmark non-discriminativeness already documented (Design-Bench critiques, benchmark saturation, RaM/LTR, PGS AAAI 2024, GAMBO NeurIPS 2024, Match-OPT)? What are the known criticisms and limitations of Design-Bench as a benchmark, of deep-ensemble uncertainty quality, and of ANOVA-on-normalized-scores effect sizes in ML benchmarking? What do AAAI reviewers demand of measurement/null-result papers, and what is the acceptance evidence for "controlled study without a new method" papers at AAAI/NeurIPS/ICML?

Severity-rate every finding: P0 (reject-driver), P1 (major revision demand), P2 (minor), P3 (polish). Each finding needs evidence, why a reviewer raises it, cost to fix (hours/CPU), and whether it is fixable pre-deadline or must be absorbed as a limitation. Also run at least one adversarial search on criticism/limitations of each core claim.
