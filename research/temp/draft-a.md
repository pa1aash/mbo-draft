# Decomposing the GP Advantage in Offline MBO — Adversarial Audit (Draft A)

**The answer first.** The paper's load-bearing novelty claim survives, but narrowly, contingently, and for a weaker reason than the paper gives. No prior work runs a crossed surrogate-model-class x optimizer factorial with a two-way variance decomposition in the offline setting: N6 is **CONFIRMED NONE-FOUND**, after a 2026 recency sweep, a forward-citation walk over ~347 citing papers, and a first-time sweep of three regions where the analysis was *more* likely to exist than in offline MBO [1][2][3]. But the claim now rests on scoping rather than absence, one of its three defensive grounds was falsified on contact with a competitor's own appendix [4], and one paywalled lead whose title names three surrogate classes *and* two optimizer families remains unread.

**The citations are in far worse condition than the numbers.** Twelve mandatory fixes follow; four are severe. `demsar2006statistical` is cited for a numeric threshold that does not exist in the source, and the citation **inverts the source's actual reasoning** [5][6]. `fan2024minucb` is cited three ways and all three fail, including one where a **convergence theorem is cited as authority for a failure mode** [7]. `liu2020sngp` is cited to bound a claim its own Figure 1 asserts the opposite of [8]. And the claim that the ensemble-size sweep "extends below" the prior range is **false** — Li, Rudner & Wilson tested K=2 and reported robustness there [9].

**The asymmetry is the finding.** Every quantitative claim checkable against the repository's artifacts reproduced exactly: four corner point estimates and intervals, the corner range, interval widths, Elimination 1's seven figures including a paired bootstrap delta, three inversion counts, both frozen-cell claims *to the individual seed*, and two code-level line traces. Exactly one integer does not — "five other cells tie it" should be six. So eleven mandatory items concern what a *source* says and one concerns an integer recount. **None concerns a number the paper computed.** A paper that verifies `1.5287419557571411` across sixteen seeds should not be attributing a numeric threshold to a JMLR paper that does not contain it. That gap — meticulous with its own evidence, careless with everyone else's — is the most actionable thing this audit can report, because it says the remaining pre-deadline effort belongs entirely in related-work and scoping prose and not in re-running anything.

---

## N6 Verdict: The Crossed Surrogate x Optimizer Factorial

**Verdict: CONFIRMED NONE-FOUND, with one paywalled lead unresolved.** No paper found satisfies all four conjuncts simultaneously: two or more genuinely distinct surrogate **model classes**, two or more optimizer routines, the two **crossed**, and a **two-way variance decomposition**, in an offline or fixed-dataset setting. The first contribution stands. What follows is why it stands more narrowly than the paper claims.

### A. The queries, run verbatim

Eight offline-MBO/BBO papers from January-June 2026 were fetched in full and grepped for twelve decomposition terms (`factorial`, `crossed`, `ANOVA`, `analysis of variance`, `eta squared`, `main effect`, `variance decomposition`, `two-way`, `interaction effect`, `attribution`, `OFAT`, `simple effects`). **Zero hits, all eight papers, all twelve terms.** SPADE ablates only its surrogate backbone against a fixed evolutionary-plus-LCB optimizer, one axis at a time [10]; the diffusion-LLM paper's ablation is leave-one-component-out rather than a crossed 2x2 [11]; the ranking-learnability paper varies training loss across five Design-Bench tasks with **a single fixed gradient-ascent optimizer in every experiment** [12]; SOO-Bench's contribution is a stability indicator tracking degradation against the offline dataset, not variance attribution [13]. The subfield's own 2026 survey returns zero hits across 25,965 words — it reviews the literature, it does not run the experiment [14].

The forward-citation walk covered ~347 citing papers across Hutter 2014 (62 citers, 2023-2026), Liang 2021 (166), Moosbauer 2022 (4, all years) and van Rijn & Hutter 2018 (115), with six candidates deep-verified and two fetched in full [1]. EXPObench compares six surrogate-assisted algorithms with zero ANOVA hits, and its "offline" means offline surrogate *training*. Bischl et al.'s 2023 HPO survey — at 836 citations the highest-cited Moosbauer citer — returns zero hits and cites Moosbauer only as related work. PED-ANOVA, the quantum-neural-network fANOVA lineage, CART-ANOVA transfer learning and Instance Space Analysis all extend functional ANOVA *within one algorithm's hyperparameter space*.

Then three previously-unsearched regions, chosen because a crossed factorial with ANOVA was *more* likely there than in offline MBO: surrogate-assisted evolutionary computation, AutoML/CASH/kernel selection, and simulation-optimization. The most on-target hit in the community with the strongest design-of-experiments culture — Kudela & Dobrovsky's comparison of eleven surrogate-assisted evolutionary algorithms — returns **zero hits on all nine decomposition terms across 47,342 characters**, because it compares eleven *bundled* method identities by Friedman and Wilcoxon rather than crossing a surrogate factor against an optimizer factor [2]. **The community best positioned to have already run this analysis has not run it.**

### B. The near-misses: all re-confirmed, none extended

| Near-miss | Status | Disqualifying detail |
|---|---|---|
| Hutter 2014 (fANOVA) | NEAR-MISS confirmed | One-way; Table 1 caption reads "based on data from one SMAC run." Zero factorial/crossed/two-way/offline hits [1] |
| Liang 2021 (npj Comput Mater) | NEAR-MISS confirmed | Genuine surrogate x acquisition grid, zero ANOVA/eta-squared hits; comparison by two custom descriptive ratios against random search; explicitly "closed-loop active learning" [15][16] |
| Moosbauer 2022 (IEEE TEVC) | NEAR-MISS confirmed | Names fANOVA and declines it — "any interactions between inputs cannot be detected by an OFAT analysis" — on cost grounds [1] |
| **RaM Table 3** (Tan et al., ICLR 2025) | **NEW, and the closest that exists** | A genuine 9x2 crossed grid *in offline MBO*, reported descriptively; zero decomposition language across 172,006 characters [4] |
| DiBO Table 2 (2026) | NEW, weaker | Genuine crossed 2x3, but a training *stage* is not an optimizer routine applied to a frozen surrogate [17] |
| BOOST (AutoML) | NEW, structurally closest | Crosses kernel x acquisition with an isolating ablation, but Matern/RBF/RQ are three covariance functions inside **one** GP class, and its "offline" denotes a no-extra-query step inside an online loop [3] |

**A methodological note.** Liang's full text returns fourteen hits for `crossed` — every one the **"Crossed barrel" dataset name** [15]. This audit caught four homonym traps of the same family: *Crossed barrel*, the surnames *Usmanova*/*Bozhanova* in shared bio-design bibliographies, *Toutanova* inside a BERT citation, and BOOST's false-friend "offline." A grep-count-only methodology would have produced false positives in five separate papers. The verdict rests on reading the hits, not counting them.

### C. Why the survival is contingent

**One of the three defensive grounds was falsified on contact.** The residual is defended against RaM on three grounds: loss type is not model class; the nine Table 3 methods are bundled rather than a clean optimizer factor; and reporting is descriptive. The second is **false as stated**. RaM's own Appendix E.5 describes four of the nine — BO-qEI, CMA-ES, REINFORCE, Gradient Ascent — as *"baselines that optimize a trained model"* [4]. That is precisely the clean optimizer sub-grid the ground claims does not exist. **A 4x2 crossed optimizer x training-loss sub-grid is present inside RaM Table 3, in offline MBO, published before this submission.** The ground survives only for the other five: CbAS and MINs are genuinely aliased in the technical sense — model family and search paradigm co-occur on every run and cannot be separated by the design's contrast structure [18] — and Tri-Mentoring, PGS and Match-OPT bundle extra machinery.

**Worse, the audit proved while defending N6 that the decomposition is derivable from a competitor's published table.** RaM reports mean and standard deviation per cell over a uniform eight seeds, so both moments and N are available for all ninety cells; a per-task two-way ANOVA on those published numbers takes about fifteen lines of code and yields a loss-axis eta-squared of 0.027 against a method-axis eta-squared of 0.577 [4]. This **strengthens** "no prior work *reports* a crossed surrogate x optimizer decomposition" — still true — and **destroys** "no prior work *could have*," which this audit disproved in under a day. N6 must therefore be stated as a claim about what the literature *reports*, never about what the design space permits. The contribution is running the decomposition as the **primary analysis on a purpose-built grid**, not the arithmetic itself.

**And the grounds are nowhere on the page.** The introduction files `tan2025ltr` under "surrogate-class comparisons hold the optimizer constant," beside `li2024bnnsurrogates` [9]. That is accurate about RaM's *main* experiment — the five-surrogate Spearman comparison genuinely holds gradient ascent fixed — so the sentence is **incomplete rather than false**, and the distinction matters. But it is incomplete in exactly the direction that makes RaM look safely outside the residual before a reader checks Table 3. A reviewer reading only the current prose will conclude the authors either did not read RaM's appendix or hoped nobody else would.

The distinction the defence needs is statable: **a change is a model-class change if it cannot be instantiated without also changing the estimator family** — a GP posterior requires marginal-likelihood fitting, an ensemble requires per-member MAP training — **and a training-objective change if it can be applied while holding the family fixed**, as RaM does by swapping MSE for ListNet inside one unchanged MLP [4]. This has domain precedent: the field's canonical uncertainty-quantification survey makes method family the top-level taxonomy and catalogues loss function as a separate cross-cutting axis in its own tables [19]. It is self-consistent, since it correctly classes COMs — a regularizer on a fixed architecture — as a training-objective study, exactly as the introduction already does. And the empirical asymmetry supports it: if loss and model class were the same kind of factor, swapping one would not move 2.7% of variance while swapping the other moves the dominant share [4].

### D. The required fix

1. **Relocate `tan2025ltr`** out of the "surrogate-class comparisons" bucket into a **training-objective comparisons** bucket alongside `trabucco2021coms`, which is what RaM is.
2. **Add a footnote naming RaM Table 3** as the nearest near-miss, with the four-versus-five breakdown and the 0.027-versus-0.577 observation.
3. **Restate N6 as a claim about what the literature reports**, in the sentence itself.
4. **Name `lu2022revisiting`**, currently uncited in the paper's own bibliography. Full-text grep returns zero for all eight decomposition terms across 37 pages; it varies uncertainty-penalty heuristics while holding the policy optimizer fixed, flagging MOReL's differing optimizer only as an unresolved implementation confound in an appendix. Naming it *strengthens* the no-prior-instance claim by showing the adjacent field's closest attempt also held an axis fixed — and a reviewer who sees it in the bibliography but not the text will ask why.

**This is a stronger position than the paper occupies**, because it concedes the real thing and defends the actual residual: the model-class axis, which RaM never varies.

### E. The lead still open

**Elsayed & Lacor (2014), "Robust parameter design optimization using Kriging, RBF and RBFNN with gradient-based and evolutionary optimization techniques,"** *Applied Mathematics and Computation* 236:325-344, DOI 10.1016/j.amc.2014.03.082. The title names **three surrogate classes and two optimizer families** — the only title in this audit naming both axes. Primary text could not be obtained: ScienceDirect, Unpaywall (no OA location), Crossref, Semantic Scholar (abstract elided by the publisher), Academia.edu, ResearchGate and the institutional repository were exhausted, and no verdict was rendered from snippets. The question is precise: a **sequential** design (surrogates compared, then one winner optimized two ways) is not a kill; a **genuine crossing with a variance decomposition** would be. This belongs in the N6 verdict itself, not in a limitations paragraph, because the author can resolve it through institutional access in minutes and the reviewer who finds it first will not be charitable.

---

## (i) Claims the Literature Contradicts or That Are Miscited

Ordered by severity descending. For each: the claim, the paper's framing, what the source says, and the fix.

### 1. `demsar2006statistical` — a threshold that does not exist, attached to inverted reasoning

**The claim** (`main.tex:220`, echoed in the contributions list): *"we are below the threshold for the test we ran: this omnibus is recommended for more than ten datasets."*

**The source contains nothing of the kind.** The full thirty-page JMLR paper was extracted and grepped directly [5]: `more than ten` = **0**, `at least ten` = **0**, `ten or more` = **0**. The only two occurrences of "ten data sets" describe **Demsar's own power-simulation sampling** — "samples of ten data sets were randomly selected," "1000 random selections of ten data sets" — the setup of his simulation study, not a usage threshold.

**This is not an over-read of something stated loosely. It is a specific numeric recommendation attributed to a real source that does not make it**, in a paper whose entire rhetorical position is that unstated protocol details are the field's central methodological failure.

**And it inverts his reasoning, which matters more.** Demsar recommends *"a set of simple, yet safe and robust non-parametric tests... the Friedman test with the corresponding post-hoc tests for comparison of more classifiers over multiple data sets"* — with no dataset-count floor anywhere [6]. His stated design context is explicitly the small-n regime: *"the number of data sets is usually much less than 30."* **He recommends the non-parametric route precisely because n is small.** The paper cites him as establishing the omnibus needs n > 10 — the opposite. His own Figure 5 independently shows Friedman as *more* powerful and more replicable than ANOVA at small samples [5].

**Fix.** Delete "recommended for more than ten datasets" and the Demsar citation from that clause. The conclusion is unaffected and better supported without it: the power limitation is independently established by the same sentence's paired-test calculation (|d_z| >= 1.27 for 80% power at alpha = 0.05, n = 7) and by Agarwal's never-claim-equivalence point, which is verified verbatim and correctly used [20]. **The fix removes a false warrant and an unnecessary self-deprecation at once** — the paper was conceding a methodological weakness it does not have.

### 2. `fan2024minucb` — three uses, three failures, including a convergence result cited as authority for a freeze

Fan, Wang, Ng & Hu, NeurIPS 2024. Full text extracted and grepped across 1,550 lines [7].

**2a. "UCB-as-local-search" is not established prior work.** The paper writes *"as is the reading of a UCB-style acquisition as local search"* — presenting it as settled. Fan et al.: *"we **propose** our first algorithm... MinUCB, which **replaces** gradient descent step with a step that minimizes the UCB in GIBO"*, and *"This discovery... **opens up possibilities for new designs**"* [7]. A method proposed in 2024, not a reading the field established. The underlying two-stage premise traces to GIBO, which Fan et al. explicitly modify — and **GIBO's exploitation step is plain gradient descent with no confidence bound anywhere**; its own "local search" language refers to using the function's local derivative, a different sense entirely [21].

**2b. The LCB-paralysis citation inverts the source's central result — the most serious of the three.** The paper: *"The surviving mechanism is LCB paralysis: the GP's lower confidence bound is locally maximal at the data, so the optimizer never leaves... This is the offline instance of a known reading of UCB-style acquisitions as local search, applied rather than discovered."*

Full-text grep of Fan et al.: **zero hits** for `offline`, `LCB`, `lower confidence bound`, `stuck`, `frozen`, `freeze`, `paralysis` [7]. They define UCB(x) = mu(x) + beta*sigma(x) and **minimize** it because their objective is minimization; they never treat a maximization setting and never mirror the transform. Decisively: **their Theorem 1 proves MinUCB converges at a polynomial rate to a genuine local optimum**, contingent on an *increasing* beta schedule and *continual active resampling every iteration* — both structurally impossible on a static offline dataset with no further oracle queries. The paper cites this convergence result as the authority for an "optimizer never leaves" **failure** mode. That is not a stretched source; it is a source pointing the other way.

**Fix.** Drop `fan2024minucb` from the LCB-paralysis sentence. **This costs nothing and removes a reviewer's cleanest kill**, because the paper's own evidence is stronger than the borrowed authority: bit-identical constants across sixteen seeds, two GP classes, a continuous task with no argmax decode.

**A correction this audit owes itself.** An earlier pass concluded "nobody owns the trapped-at-the-data failure mode, so claim it outright." That was reached by *excluding* three candidates — TuRBO, which diagnoses the opposite pathology of over-exploration; Fan, which proves convergence; GIBO, which uses no confidence bound [21] — without sweeping the vocabulary. Swept, **Yarotsky (2013), "Examples of inconsistency in optimization by expected improvement,"** *J. Global Optimization* 56(4):1773-1790, is the closest prior formalization in existence: Theorem 3 proves that EI-driven BO started at the true optimum has a trajectory converging back to that point and "not dense." It differs on three real axes — expected improvement rather than a confidence bound, online rather than offline, an adversarial start rather than a seed-robust observation. **The "no prior art" phrasing is withdrawn; the paper owes Yarotsky a scoping sentence.** Two adjacent candidates are genuinely different: Ament et al. (NeurIPS 2023) diagnose a *numerical* vanishing-gradient pathology specific to EI, and Kim & Choi bound the regret gap between local and global acquisition optimizers via a collapse probability — touching GP-UCB but concerning within-round inner-optimization fidelity, not the outer-loop claim [22].

**2c. The distance-aware co-citation is unsupported.** *"Nor do we claim... distance-aware uncertainty as ours \citep{liu2020sngp,fan2024minucb}."* Fan has **zero hits** for `distance-aware` and uses vanilla GP posterior variance [7]. **Remove it from that co-citation.**

### 3. `liu2020sngp` — cited to bound a claim its own Figure 1 contradicts

**The claim:** *"sigma is a distance signal, not an error signal... this is the corrected measurement, **bounded by prior work on distance-aware uncertainty** \citep{liu2020sngp,vanamersfoort2020duq}."*

**SNGP argues the opposite, about the exact model class at issue.** Verbatim: *"deep ensembles... are based on dense output layers that are **not distance aware**. As a result, both methods quantify their predictive uncertainty based on the distance from the decision boundaries, **assigning low uncertainty to OOD examples even if they are far from the data**"* [8]. SNGP's Definition 1 formalizes input-distance-awareness as a property standard deep models **lack**, and that lack is its entire motivation for a GP output layer plus spectral normalization. The same group's follow-up continues the program on the same premise, arguing softmax cross-entropy "undoes the distance constraint" [23]. DUQ corroborates: *"Deep Ensembles is uncertain only along the decision boundary, and certain elsewhere."*

**A second, independent scope problem: all SNGP experiments are classification** — a 2D toy, Wide-ResNet on CIFAR, ImageNet, BERT. **Zero regression experiments** [8]. The paper is regression throughout.

**Fix.** The claim cannot be "bounded by" SNGP. Either drop the bounding citation and present rho ~ 0.26 as a bare measurement, or — better — **reframe as a contrast**: "unlike the ensembles SNGP and DUQ characterize as distance-unaware, ours shows a modest positive distance correlation." That is weaker as an appeal to authority and considerably more interesting as a result. **A finding that runs against SNGP is more publishable than one leaning on it, provided it is labelled as such.**

### 4. The sigma dichotomy is contradicted — in regression, peer-reviewed

**The direct counterexample.** Carrete et al. 2023, *J. Chem. Phys.* 158:204801, neural-network force fields, a regression task: *"the Spearman correlation coefficient between uncertainty and error over the validation data set is **0.90 for the committee and 0.91 for the bootstrap-aggregation ensemble**"* [24] — roughly **thirteen times** the paper's rho ~ 0.07. A second, independent domain: deep-ensemble uncertainty correlates with per-pixel prediction error at Pearson 0.715 in seismic-facies segmentation [25].

**But the resolution is scoped, not a rout, and the paper should say so precisely.** Carrete's 0.90 is on validation data from the *same* distribution as training; on their genuinely out-of-distribution test the per-atom correlation degrades while remaining informative in aggregate [24]. **The load-bearing variable is where uncertainty is measured.** In-distribution, distance and error largely coincide. Far off-support, the SNGP/DUQ counterexamples bite, because a dense output layer measures distance from a decision boundary rather than from the data manifold [8].

**Fix, two parts.** (a) Scope the framing to this grid — "on our tasks, sigma tracks distance more than error." (b) **Drop the dichotomy.** The literature does not treat distance-awareness and error-tracking as exclusive; SNGP explicitly ties distance-awareness to *both* calibration and OOD detection [8]. The paper's "not" is a false opposition — a framing fix independent of the numbers.

**A live confound the paper never examines.** Lakshminarayanan et al. explicitly contrast their NLL-trained heteroscedastic ensemble against *"a commonly used heuristic... to train an ensemble of NNs to minimize MSE and use the empirical variance of the networks' predictions as an approximate measure of uncertainty,"* showing the naive variant **"consistently underestimates the true uncertainty"** — an 80% nominal interval covering ~20% of test points [26]. **The paper's ensemble is exactly that construction**: K=5 MLPs, MSE, cross-member empirical variance. Note also that on their 1D regression toy the NLL-trained ensemble's predictive standard deviation *does* grow with distance, so ensembles are not categorically distance-blind either.

**This audit over-generalized on first pass and corrects itself.** "MSE-trained ensembles underestimate uncertainty" is too strong. Ghasemipour et al. treat standard init-seed-diversified deep ensembles — this paper's construction — as the **reliable reference**; what fails in their study is weight-shared "efficient" approximations, which this paper does not use [27]. Ovadia's large-scale benchmark finds deep ensembles the **best** UQ method under dataset shift, though entirely in classification, with zero occurrences of `correlat`, `distance` or `regression` in the text [28]. D'Angelo & Fortuin note standard ensembles "do not offer any guarantees for diversity" and can saturate toward single-model behaviour — yet their own Table 1 shows standard ensembles winning OOD detection in one setup, so this is not a uniform indictment [29]. Fort et al. establish why diversity arises (independent initializations reach different loss-landscape modes) but measure function-space diversity, not input-distance awareness, so they neither support nor undercut the rho figures [30].

**What survives, precisely:** the MSE-versus-NLL comparison inside Lakshminarayanan's own paper is a real, in-scope caveat about sigma *magnitude* calibration for exactly this construction. It is **not** a general finding that init-seed ensembles are unreliable. Ensemble construction remains a live, unexamined alternative explanation for rho = 0.07, worth one scoping paragraph phrased as "this specific sigma construction has a known calibration caveat," never as "MSE ensembles are broken."

### 5. `li2024bnnsurrogates` — the "extends below" claim is false

**The claim (Confound 3):** *"Our sweep runs over K in {2,3,5,10} and therefore **extends below** the K in {5,10} range over which ensemble surrogates were found robust, **sharing its two upper points and adding K=2,3**."*

**Figure A.7 tests K = {2, 5, 10}.** Verified first-hand rather than by relay: arXiv:2305.20028v2 was fetched directly (39 pages, 111,042 characters) and the Figure A.7 legend tokens extracted from page 28 read `['2 Models', '5 Models', '10 Models']`; a second route reached the same result by rendering that page as an image. Their verbatim finding from the same caption: *"the different ensembles perform similarly across many experiments, showing the robustness of our results to this hyperparameter."* A second K=2 appearance is in Figure A.5, and the main text adds these hyperparameters "generally have minimal effects on the performance" [9].

**So the range is {2,5,10}. They already went to K=2 and found robustness there. The claimed residual collapses from "adding K=2,3" to "adding K=3."**

**Why this is worse than a wording slip.** The K-sensitivity framing exists to establish that the headline sits at the *maximum* of a sensitive curve. Li, Rudner & Wilson is the closest prior work on exactly this question and reports the opposite conclusion over an overlapping range including the very endpoint the paper claims to have contributed. A reviewer from that group — a live possibility in a community this small — reads the sentence as the paper claiming credit for their experiment.

**Fix, and it must be a real fix.** (1) Correct the range to K in {2,5,10} everywhere. (2) Restate the residual honestly: the paper adds K=3 and, far more importantly, reports **eta-squared sensitivity** where they report **performance robustness**. Those are different quantities — they measure whether ensemble BO *performance* changes with model count; the paper measures whether the *variance attributed to the surrogate axis* changes. **That distinction is the paper's actual defence and it appears nowhere in the text.** Made explicit, the claim survives in a narrower and more interesting form. (3) Do not delete the K-sensitivity result — it is real. Delete the priority claim. Incidentally the year trap is clean: the arXiv comments field reads "ICLR 2024" against a v1 date of 31 May 2023, and the paper has it right [9].

### 6. `abe2022ensembles` — cited for a direction the source never measures

**The claim (Confound 3):** *"the decline with K runs against **the direction reported for ensemble quality**."*

**Abe et al. run zero ensemble-size ablation.** Full text extracted, 15,095 words. "Deep Ensembles Work, But Are They Necessary?" fixes M=4 on CIFAR-10 ("combining 4 out of the 5 random seeds") and M=5 on an ImageNet subset — fixed by seed availability, not swept. Its thesis is *ensembles versus a single larger model*. **There is no reported direction-with-K in that paper to run against.**

**Fix.** Replace with the source that does report it: Lakshminarayanan, Pritzel & Blundell, Table 4, ImageNet M = 1 to 10, monotonic diminishing-but-never-negative improvement (Top-1 error 22.166% to 18.675%), verbatim *"as M increases, both the accuracy and the quality of predictive uncertainty improve significantly"* [26]. State the caveat: that is classification calibration, not offline-MBO reward, so the cross-domain transfer must be argued rather than assumed.

### 7. `rahaman2019spectralbias` — motivates the wrong ablation, and the architecture makes it worse

**The claim (Elimination 2):** *"wide networks approach GP behaviour \citep{jacot2018ntk,lee2018dnngp,lee2019widenets} and **finite networks show spectral bias** \citep{rahaman2019spectralbias}. ... so we ran the ablation that does. At fixed K=5, sweeping per-member **width** over a 10.7x range leaves the gap unchanged."*

**Three of four are clean, and the audit should say so.** Jacot (NTK, training dynamics), Lee 2018 (NNGP, at-initialization) and Lee 2019 (wide nets as linear models under gradient descent) collectively *do* support "wide networks approach GP behaviour," covering the at-init and during-training regimes; all were re-fetched fresh [31][32]. This is the cluster most likely to be assumed sloppy and it holds up. The only precision gap is that Lee 2019's own text notes the gradient-descent-limit GP is "noticeably different" from the NNGP Bayesian posterior — elided, not falsified.

**The fourth is miscited in effect.** Rahaman's theorem holds *"for arbitrary width and depth"* — not a width-limit statement at all — its defining experiment varies training *iterations* rather than width, and its own controlled ablation finds *"increasing the depth (for fixed width) significantly improves the network's ability to fit higher frequencies... increasing the width (for fixed depth) also helps, but the effect is **considerably weaker**"* — despite width scaling exponentially (16 to 128) against depth's linear increase (3 to 6) [32]. The paper cites spectral bias to motivate a **width** ablation when the source says width is the weaker lever, and the sentence structure ("wide... **and** finite...") invites a width-monotonic reading the source argues against.

**The architecture compounds it.** `main.tex:88` fixes the ensemble at a **two-hidden-layer MLP across the entire width sweep** [32]. **The paper varies the axis Rahaman calls weak and holds fixed the axis Rahaman calls dominant. The width-only ablation cannot have tested the depth-driven spectral-bias mechanism it invoked Rahaman to motivate.**

**Fix — the second option is better.** (1) Drop `rahaman2019spectralbias` from that sentence; the width objection stands on the NTK/NNGP lineage alone. (2) Keep it and state the limit: spectral bias is reported as primarily a *depth* phenomenon, the ablation sweeps width at fixed depth 2, and the depth-driven version of the objection **remains open**. One clause converts a miscitation into a correctly-scoped limitation, names a follow-up, and pre-empts a reviewer who knows Rahaman.

**Note the asymmetry with Elimination 2's own framing.** The paper is scrupulous that the sweep "stops at 1024 while the kernel limits are asymptotic, so this answers the objection at practical widths and makes no asymptotic claim." That scrupulousness was not applied to depth. This matters for the headline count: **Elimination 2 is eliminated at fixed depth 2, not eliminated**, so "seven controls, none survives" does rhetorical work at least one underlying control does not fully support.

### 8. `melis2018sota` — one instance asked to certify a genre-wide law

**The claim, made twice and load-bearing for Contribution 2:** *"Audits in this genre **usually shrink** the effect they audit"* and *"...where audits normally shrink."*

**Melis reports a ranking reversal, not a shrinking effect size.** Full text verified: *"we find that LSTMs outperform the more recent models, contra the published claims"* — an old baseline beating newer architectures, and certainly not a law about a genre's direction [33]. Melis explicitly frames the work as one instance: *"this paper joins other recent papers in warning of the under-acknowledged existence of replication failure in deep learning."* Full-text search finds **no sentence anywhere** asserting a genre-wide directional pattern.

**Fix, nearly free.** Recast as an observation over named instances — Melis, Ferrari Dacrema, Musgrave and Lucic each independently show a *claimed advantage* shrinking or reversing — and cite all four for the pattern rather than one for a law [34]. The paper already cites all four in its genre paragraph, so this costs a comma. As written, **one citation is asked to certify a claim about a literature, which is the exact defect this audit was commissioned to find.**

### 9. `ghasemipour2022pessimistic` — an over-extended analogy where the accurate version is more useful

**The claim (Confound 4):** the cross-model-class incomparability principle is owned by the interval-validity literature *"and **its offline-RL instance** \citep{ghasemipour2022pessimistic}."*

**Their entire scope is one surrogate class, Q-ensembles.** Their axis of comparison is an *internal training-procedure* choice — shared versus independent Bellman targets, full versus weight-shared ensembles. **They never compare across distinct model families**, so they are not an instance of a cross-model-class principle [27]. Their actual finding is stronger and different in kind: shared pessimistic targets can flip the LCB's **sign**, rendering an ensemble paradoxically optimistic, rather than merely rescaling an interval.

**Fix.** Drop the "offline-RL instance" framing or restate it accurately: *a training-procedure choice within one surrogate class can invalidate, not merely miscalibrate, nominal pessimism.* The accurate version is more useful, because the same source supplies the candidate positive mechanism for the inversion result (rank 9 below).

**`dewolf2022intervals` is verified clean and correctly narrow.** Its finding that uncalibrated methods across four regression-UQ families do not deliver matched effective coverage at a shared nominal level genuinely supports the principle the paper cedes to it, and grep confirms **zero hits** for `acquisition`, `Bayesian optimization`, `pessimis`, `lower confidence bound` across ~23,000 words — so the paper's claim to the offline-MBO acquisition application as its own residual is correctly scoped [35]. **No fix needed.**

### 10. `kim2025mbosurvey` — a stale bib entry and a contextual over-read

**Venue resolved in the paper's favour.** Every page header of v2 (6 January 2026) reads "Published in Transactions on Machine Learning Research (01/2026)"; the arXiv comments field reads "Accepted to TMLR 2026 (Survey Certification)," independently corroborated by a 2026 paper's bibliography [14]. **So the prose calling it "the subfield's 2026 survey" is accurate and the bib entry — `journal={arXiv preprint arXiv:2503.17286}, year={2025}` — is stale.** Fix: update to TMLR 2026; as written the paper contradicts itself between prose and bibliography.

**The over-read is minor but real.** The quoted sentence about benchmarks not clarifying "whether observed gains stem from superior surrogate modeling, improved optimization strategies, or mere chance" is verbatim — but it sits in Section 6 under "Uncertainty Estimation of Surrogate Model," framed around **uncertainty-quantification evaluation gaps**, not as a call for a crossed factorial [14]. The paper presents it as *"the field certifying that the attribution is unresolved."* **Keep the quote, drop the implication that the survey calls for this specific design.**

### 11. The N9 "no precedent" claim is bracketed on both sides

**The narrow scalar claim survives, and is better evidenced than when the paper made it.** An exhaustive fresh sweep in this exact framing — fourteen verbatim queries across the arXiv API and targeted search, including the ML Reproducibility Challenge and ReScience C as the natural homes for such a result — returned **zero grow-direction hits**, and surfaced a third directly-on-target shrink-direction reanalysis (Robinson, Glen & Lee, *J. Comput. Aided Mol. Des.* 2020, reanalysing Mayr et al.'s ChEMBL benchmark) alongside Musgrave and Ferrari Dacrema.

**But two flanks are exposed.** *(a)* Hamdan et al., verbatim: *"this common approach to confound removal **biases ML models, leading to misleading results**... **can leak information such that what are null or moderate effects become amplified to near-perfect prediction**"* [36]. Not a novelty kill — different genre — but worse in one respect: **in the one ML literature where "confound removal increased an effect" has been studied, the default diagnosis for that direction is leakage.** The paper must **affirmatively rule out confound-leakage**, and it can cheaply: its two corrections are a target rescaling and a candidate-selection rule, neither of which regresses a confound out of features, which is the mechanism leakage requires. But that argument must be **made on the page**, or a reviewer who knows Hamdan supplies the leakage explanation for free.

*(b)* Maassen et al. (PLOS ONE 2020) recomputed 500 primary effect sizes across 33 psychology meta-analyses: *"we estimated **19 pooled effect sizes to be larger** than originally reported and **14 to be smaller**."* Corrections go up about as often as down. This does not kill a claim scoped to "the ML reality-check literature," but it kills the *implication* that an upward correction is intrinsically surprising. The paper half-anticipates this, so the fix is a citation at that sentence, converting a hedge into a supported statement.

### 12. The conformal proposition is formally correct and practically inert — and the paper never says so

**This was initially recorded as the audit's most serious finding, on a relayed report that the proposition omits a load-bearing hypothesis. Reading the supplement directly showed the report was overstated, and the earlier framing is withdrawn** — propagating an overstatement is precisely the failure this audit exists to catch.

`supplement.tex:48` reads: *"Under a shifted proposal Pi != P **with density ratio w = dPi/dP**, validity is restored by weighting the calibration quantile by w."* It defines w as **the true density ratio**, not an estimate. So the statement is exactly Tibshirani et al.'s Theorem 2 and is **formally correct as written. The proposition is not missing a hypothesis and it is not wrong.**

**What is true, and still worth a fix.** The clause is correct and **inert in the paper's own setting**. Tibshirani et al.'s guarantee holds for **known** w; their abstract separates the estimated case (*"known — or, in practice, can be estimated accurately"*) and supports it **empirically only** — roughly 91.0% against 90% nominal on airfoil data — with **no theorem bounding the coverage gap under an estimated weight** [37][38]. Angelopoulos & Bates corroborate: *"This algorithm addresses a somewhat restricted case—that of a known covariate shift"*, and weighted procedures are *"exact when the magnitude of the distribution shift is known"* [39]. Here Pi is the distribution of designs an optimizer proposes after ascending a surrogate — the setting where w is least knowable and arguably not well-defined, since the proposals are the output of a deterministic optimization concentrated on a low-dimensional set. Nothing in the paper estimates w or applies weighted conformal.

**Fix (one or two sentences), plus a free win.** Note after Proposition 2 that the clause requires a known or reliably estimable w, that none is available for optimizer-generated proposals, and that it is stated for completeness rather than applied. **Then take the win:** the supplement already reports conformal repair *"restores in-distribution coverage to its 0.90 target on every task but **leaves OOD coverage erratic**"* — 0.00 on Styblinski, Griewank, UTR, Ant and D'Kitty. That is *exactly what one should expect* when the precondition is unavailable. Saying so converts an unexplained table into a correctly-scoped negative result. **Severity: moderate, not severe — a scoping omission around a correct proposition, ranked below the Demsar fabrication and the Fan inversion.**

### 13. Bibliography integrity — the unusual defect, and three defects in one entry

Regex over every `\cite[tp]?` in `main.tex` and `supplement.tex` against `@type{key,` in `references.bib`: 67 entries, 41 distinct keys cited, **zero undefined keys** — a genuine pass, worth stating because dangling `\citep` is the failure mode that most often survives to camera-ready. **But 26 entries are cited nowhere and four are not harmless. The pattern is the opposite of the usual bibliography defect: the paper's own bibliography contains papers that would strengthen it, uncited.**

- **`fannjiang2020autofocused`** is the sharpest missing-related-work item. Fannjiang & Listgarten (NeurIPS 2020), verbatim: *"oracle-based design... **will query the oracle in regions of the design space that are not well-represented by the oracle training data**... **its outputs, including its uncertainty estimates, become unreliable beyond the training data**."* That is the founding articulation, from 2020, of substantially the paper's own central diagnosis — including the uncertainty-estimates clause. It kills nothing, but it dents any implicit novelty in the *diagnostic vocabulary*.
- **`gao2022reward`** carries **three** defects: the key says 2022, the entry's own `year` field says 2023 (key and field disagree inside one entry), and the page range is wrong — `10909--10934` against the true PMLR 202:10835-10866.
- **`lu2022revisiting`** and **`eriksson2019turbo`** are uncited and both bear on load-bearing claims (see the N6 fix and item 2b).

### 14. Internal arithmetic — one integer, two presentation gaps, a biased statistic, and a fifth coordinate

**14a. "Five other cells tie it" should be six.** The paper's own stated criterion (`inversion_rate == 1.0` AND `mean_frac_worse == 1.0`) applied to `results/x0_inversion.json` at the audited engine returns **seven** cells: Branin ens:grad plus six others (Branin ens:cma; Styblinski ens:cma, ens:grad, ens:perturb, svgp:cma, svgp:grad). Severity is low and the direction unaffected — but it is a bare integer a reviewer with the released artifact checks in one line, and this paper's credibility posture rests on such checks passing. **It is the only figure in the entire numeric sweep that does not reproduce.**

**14b. Two paired-bootstrap deltas read as arithmetic errors.** The beta=0 increment `0.203` sits beside point estimates whose difference is 0.206; it is correct (the artifact's paired delta is 0.20321) but a reader who subtracts thinks they caught an error. The width shrinkage `-0.006` against endpoints differing by 0.004 is the same class. **One clause each saying these are paired bootstrap deltas, not differences of marginals.**

**14c. eta-squared is positively biased at n=7 and the paper never says so.** Now firsthand: Okada (2013), *Behaviormetrika* 40(2):129-147, contains the Monte Carlo bias table at one million replications per condition — eta-squared bias **+0.054 at n=10 per group**, falling to **+0.005 at n=100**, **always positive**, while epsilon-squared and omega-squared are always small and negative. Liu (2022), *Methodology* 18(1):44-57, CC BY, states firsthand that "eta squared is not an unbiased estimate and is known to have positive bias... depends on sample size," attributing corrections to Kelley (1935) for epsilon-squared and Hays (1963) for omega-squared. **Okada's simulation also overturns the folk belief that omega-squared is the least-biased correction — epsilon-squared is** — so the recommendation should name epsilon-squared, or report both. The paper's own bootstrap artifacts already contain the bias: the bootstrap mean exceeds the point estimate in **all four corners**, by +0.0099 to +0.0184.

**Fix, and it is a strengthening.** Report epsilon-squared or bootstrap-bias-corrected values alongside eta-squared. **The headline gets stronger:** bias-corrected, the two protocol confounds move the surrogate effect 0.351 to 0.395 (+0.0437) against the reported 0.367 to 0.405 (+0.0376). Direction survives; the magnitude of the *change* grows.

**14d. There is a fifth operating-point coordinate and the paper does not name it.** The paper's central methodological claim is that any headline eta-squared is a joint artifact of four coordinates — K, beta, budget, engine corner — "so a headline that does not state all four is not reproducible." **The per-task normalizer is a fifth, and arguably the most consequential**, because unlike the others it is an *analysis* choice, it is applied to every number in the paper, and its leverage scales with how outlier-dominated a task is. It is also a documented deviation from the benchmark's convention: Design-Bench normalizes to an *exogenous* range from a larger dataset, explicitly not the compared arms; the synthetic grid normalizes by the min and max of the nine compared cells themselves [40].

Two sources make this citable rather than a general worry. Jordan et al. name **this exact endogenous functional form** and its failure mode verbatim: *"algorithms could be near 0 or 1 if there is an outlier algorithm that does very well or poorly... We would like the evaluation procedure to be robust to the addition of poor algorithms"* [41]. And Bellemare et al. — the *origin* of the technique — document it flipping a ranking, in the Zaxxon case [40]. "Just use ranks" is not a clean escape: rank statistics are pool-dependent, the same class of problem, and the paper already cites Benavoli correctly for exactly that defect elsewhere [42].

**The empirical resolution, which corrects one of this audit's own framings.** A recomputation across five alternative normalizers over four corners, five beta levels and both budget levels — 33 combinations plus leave-one-task-out, run on the paper's own raw result files — finds **every one of the 33 preserves eta-squared_surr > eta-squared_opt**. Direction robust; magnitude not, with eta-squared_opt moving up to 3.4x and the interaction consistently halved under rank. **And the framing that motivated the concern was wrong: Griewank-30D, the catastrophic-outlier task, is empirically the *smallest* lever on the headline, not the largest** — dropping it moves 0.405 to 0.395, while dropping Styblinski-5D moves it to 0.485 [40]. Recorded because this audit raised the alarm on Griewank specifically. One further disclosure the check surfaced: min-max-normalized residuals reject normality and homoscedasticity while rank-transformed residuals satisfy both, so the point estimate sits on an assumption-violating scale; the nonparametric bootstrap is the right hedge, but the scale is not disclosed.

### 15. Verified clean — and this list calibrates everything above

An audit returning twelve mandatory fixes reads by default as an indictment. Stating the passes is what makes the indictment proportionate.

| Item | Status |
|---|---|
| `shahriari2016humanoutoftheloop` for the surrogate-over-acquisition doctrine | **PASS, and the paper *understates* its source** — Shahriari says it twice as the review's organizing thesis ("far more important than the choice of acquisition function heuristic"); zero hits for `offline` across 137,300 characters, so the scope restriction is accurate and non-strained |
| `chemingui2024pggs` as falsification target | **PASS.** Verbatim: *"...complements prior methods that have **focused on improving surrogate models while using fixed search strategies**."* The premise is asserted by the source, not fabricated [43] |
| Jacot / Lee 2018 / Lee 2019 cluster | **PASS** [31][32] |
| Henderson = AAAI 2018; Benavoli = JMLR 17 (2016); Li/Rudner/Wilson = ICLR 2024 | **PASS** — all three traps real, all three the paper has right [42][9] |
| Recht slopes 1.69 / 1.11; Agarwal for never-claim-equivalence | **PASS**, both verbatim, and Agarwal's usage matches almost word for word [20] |
| `dewolf2022intervals` | **PASS**, correctly and narrowly scoped [35] |
| Code-level line traces for Confounds 1 and 2 | **PASS without exception.** Every claimed line resolves to exactly what the paper says; lines 35-40 of `mbo.py` are a source comment stating the defect in *more* detail than the paper does |
| Engine-stamping protocol | **PASS.** Both checked artifacts carry platform, OS, Python, torch, numpy, botorch, gpytorch, cma, git SHA, flags, K, beta, TOP, seed range and timestamp. **The protocol the paper prescribes is one it actually followed** — the claim most likely to be assumed rhetorical, and it is not |
| Frozen cells, to the seed | **PASS exactly.** On Ant, two GP cells hit `1.5287419557571411` on 16/16 with one distinct value; the gradient cell on 0/16 (mean 1.32417, sd 0.198, matching the paper's own self-correction); `svgp:perturb` on **exactly ten of sixteen**. On TF-Bind-8, all three exact-GP cells frozen at exactly 1.0 in all four corners, with ens:perturb joining in exactly two |

---

## (ii) Kills

**No kill was found. N6 is not refuted, and no specific row of the claim ledger is refuted by any prior work located in this pass.** That is stated flatly because a single fatal counterexample would outweigh every improvement below, and none exists in the material examined. But "no kill found" is not "no kill exists," and the prosecution's obligation is to say how much weight the negative carries.

| Closest candidate | Why it is not a kill |
|---|---|
| **RaM Table 3** (ICLR 2025) | The only genuinely crossed grid in offline MBO. Fails the **model-class conjunct**: the second axis is a training loss swapped inside one unchanged MLP, reported as Score+/-std and %Gain with zero decomposition language [4]. **It is the closest thing to a kill that exists and the paper must name it** |
| **BOOST** | Crosses kernel x acquisition with an isolating ablation — structurally the closest match anywhere. Fails conjunct (a): three covariance functions within one GP class; also online, with a false-friend "offline" [3] |
| **Liang 2021** | Genuine surrogate x acquisition crossing across five domains — the closest *structural* analog to the paper's 3x3 shape. Fails on **method**: no ANOVA, no eta-squared, no significance testing of any kind, only two descriptive ratios against random search; also online closed-loop [16][15] |
| **DiBO Table 2** | Genuinely crossed, contemporaneous, offline. A training *stage* is not a search routine applied to a frozen surrogate, and reporting is descriptive [17] |
| **Kudela & Dobrovsky** | The best remaining regional bet. Eleven bundled method identities compared by Friedman/Wilcoxon; zero hits on all nine terms [2] |
| **Hamdan et al.** (against N9) | Establishes the field's default *diagnosis* for an upward de-confounding move, not a prior instance. **Not a kill, but it shifts a burden the paper has not discharged** [36] |
| **Li/Rudner/Wilson Fig. A.7** | Not a kill on N6 — zero hits for `offline`, `ANOVA`, `factorial`, `crossed`, `variance decomposition`, which *simultaneously confirms* the N6 defence. But it **is** a refutation of a specific sentence in Confound 3, and belongs in deliverable (i) [9] |
| **Yarotsky (2013)** | Not a kill on any ledger row. Closest prior art for the acquisition-stalling *vocabulary* — a citation obligation, not a retraction |

**Three limits on this negative, which belong in the paper's own limitations rather than a reviewer's report.** First, Elsayed & Lacor is genuinely unresolved and has the most N6-shaped title in the audit. Second, Semantic Scholar returned HTTP 429 for the entire session, so the forward walks ran on OpenAlex and arXiv alone; OpenAlex's full-text search is very noisy here — it returned climate models and echocardiography for optimization queries — so its nulls are weak evidence of absence and the verdict deliberately does not rest on them [1]. Third, the AutoML and kernel-selection literature was searched for a *crossed design*, not for a source treating model class and training objective as **one** factor; such a source would attack the surviving ground (1) without touching the factual record, and absent one the ground stands on Abdar's taxonomy alone [19].

**One thing the absence of a kill does not license.** The crossed design is unowned; the *questions* around it are not. The algorithm-selection tradition from Rice through SATzilla to Kerschke & Trautmann is highly developed and selects **solvers**, never surrogates [44]; a 2023 survey of the entire field, feature-based and feature-free alike, returns **zero hits for `surrogate`** [45]. That is a second independent confirmation that surrogate-class selection is not a recognized sub-problem — good news for the paper, and also a reason it cannot borrow that literature's credibility for its framing.

---

## (iii) Scope of Novelty, Ranked

Ranked by value per unit cost. Everything above rank 9 requires **zero new computation**.

### Rank 1 — Interpret the interaction term. FOLD-INTO-THIS-PAPER / CHEAP.

**The paper argues that interaction detection justifies its design, runs the design, finds a significant interaction in all four corners, prints it in a table column, and never mentions it again.** The word `interaction` appears **exactly once** in `main.tex`, inside a related-work sentence citing Moosbauer's reason for declining fANOVA [46].

eta-squared_inter is 0.165 / 0.146 / 0.152 / 0.160 across the four corners, with bootstrap intervals excluding zero in **all four** (lower bounds 0.087-0.107). It is the **second-largest effect in every corner**, ordered surrogate > interaction > optimizer without exception, at 4x-33x the optimizer main effect. And it is **more stable than the headline**: the surrogate main effect ranges 0.283-0.450 across corners (range 0.167) against the interaction's 0.146-0.165 (range 0.018) — **9.2x more stable across the one coordinate the paper demonstrates the headline is most sensitive to**. It survives bias correction at 0.134-0.156 [46].

**Why this is major, not technical.** *(a)* **It is the strongest available vindication of N6.** The entire case for a crossed factorial over one-factor-at-a-time is that OFAT cannot estimate interactions — the paper *makes that argument*, citing Moosbauer's own reason for declining. A significant interaction is the design's empirical payoff: the thing a fixed-one-axis study **could not have found**. Every near-miss above is structurally incapable of producing this number. **It is the answer to "why did anyone need to run this?"** *(b)* **It reframes the optimizer null**, currently the paper's weakest passage. The paper warns, correctly but without evidence, that a small eta-squared_opt "licenses 'optimizer choice explains little variance' and never 'optimizer choice is arbitrary' — the second reading is false on our own data." **The interaction is that evidence and it is not cited there.** The supported statement: optimizer choice has a small *marginal* effect and a substantial *conditional* one. *(c)* It is a significant positive quantitative result in a paper whose mechanism section is seven negatives — which matters at a venue bundling novelty and significance into one criterion with no audit rubric [47], unlike the segregated track stating outright that "negative results are valuable when rigorously supported" [48].

**Three independent routes reached this — two analysts and the orchestrator's own artifact work, none prompted by the others. Convergence on a quantity the paper prints four times and never discusses is itself a finding.**

**Two qualifications must travel with it.** It is **synthetic-grid-specific**: on Design-Bench eta-squared_inter is only 0.006-0.041, an order of magnitude smaller and *subordinate* to eta-squared_opt there [46]. That is consistent rather than contradictory — where surrogate cells are frozen, neither main effects nor interactions involving them can appear. And it should be promoted as an **existence-and-ordering claim** ("surrogate and optimizer are not separable; the interaction exceeds the optimizer main effect in every corner"), not as the stable quantity "0.15", because it is an eta-squared and the magnitude fragility above applies directly — halved under a rank normalizer [40]. Promoting an unqualified 0.15 would import the exact fragility this audit is simultaneously warning about.

**The positive DOE authority the paper lacks.** Its bibliography contains **zero** design-of-experiments citations — no Montgomery, no Box, no Fisher, no NIST handbook — so its only warrant for the crossed design is Moosbauer's *negative* framing [46]. Two open sources supply the positive mirror: the NIST/SEMATECH handbook's own "OFAT experimentation leaves us in the dark about factor interactions," and George Box's "Do Interactions Matter?" (1989), written to argue that interactions are practically decisive, with the SKF bearing case where "the life of a bearing can be increased fivefold if the two factors... are increased together" and the diagnosis that "because most engineers have, until recently, employed only one factor at a time experimentation, interaction effects have been missed." The formal aliasing concept is standard and the handbook supplies the definition [18].

**The honest counterweight cuts for the paper.** Sommet et al.'s preregistered metastudy of 159 psychology studies found median power to detect a typical interaction of just **.18**, with only 4% reporting adequate shape-aware power analysis, citing Gelman's rule that estimating an interaction needs sixteen times the sample size of a main effect [49]. McClelland & Judd's much-cited "less than 20% of the efficiency of optimal experimental tests" is explicitly about **field/observational** studies with restricted-range moderators — **not** designed experiments with manipulated, maximally separated levels [46]. This grid is the "optimal experimental test" case: three architecturally distinct surrogate classes against three distinct search routines. **A naive citation of "interactions are hard to detect" would misapply a field-study result to a controlled factorial** — and citing this literature correctly is a cheap display of exactly the methodological care the paper claims.

**No precedent exists for interpreting a component-by-component interaction in ML benchmarking.** Three primaries fetched and grepped across three regions: Schmidt, Schneider & Hennig's "Descending through a Crowded Valley" (ICML 2021) returns **zero hits** on all formal terms, offering only qualitative "depends on the problem" language; Choi et al. on optimizer comparisons returns **zero hits**, arguing rankings are confounded by asymmetric tuning budgets, which is not an interaction term; Steiner et al.'s "How to train your ViT?" (TMLR 2022) explicitly crosses augmentation strength x model size x data budget and calls it *"interplay"* — but **never computes a formal interaction statistic**. The ViT check is where the audit's third homonym trap fired: `ANOVA` false-matched inside *"Toutanova."*

### Rank 2 — The raw-units conditional-advantage result, posed as a question the paper tests. FOLD-INTO-THIS-PAPER / CHEAP.

Taking the GP-minus-ensemble gap in **raw oracle units** from the supplement's own `tab:sfull`: on **7 of 7 tasks the surrogate gap under perturbation is smaller than under both gradient and CMA**, averaging 5.9% of the aggressive-optimizer gap — roughly 17x attenuation. Branin 0.38 against 8.87/13.61; Ackley 0.04 against 3.11/3.72; Griewank 125.00 against 2591.06/2612.00. This is arithmetic on published numbers in raw units, so **neither the normalizer challenge nor the small-n eta-squared bias reaches it**.

A formal per-optimizer simple-effects analysis on stored per-seed data, using the paper's own shared nine-cell normalizer, independently confirms it with intervals the raw-units version lacks: **eta-squared_surr collapses to 0.007 [0.001, 0.048] under perturbation against 0.735 and 0.762 under gradient and CMA** in the primary corner [46]. Textbook methodology requires exactly this decomposition once an interaction is present — for a 3x3 design, two main effects and six simple effects. **One trap must be stated if the table is added:** re-normalizing to the three cells within one optimizer column inflates the perturbation figure to ~0.73, because min-max rescaling to any three points restores a full [0,1] spread. A correct simple-effects computation must share the omnibus normalizer [46].

**But the causal reading is not established, and this audit's own test went against it.** The competing explanation is that perturbation attenuates the gap because it *underperforms*, compressing everything, not because it neutralizes surrogate differences. **The one task where perturbation is the grid's best optimizer (Styblinski-5D, GP+Pert at 36.15) is the task with the weakest attenuation (14.3%)** — exactly what a floor effect predicts. That single point is worth more than the seven-task count, because it is the only case discriminating between the explanations.

**So the descriptive fact is solid and the causal reading is not. Fold it in as a question the paper poses and tests, never as a result it asserts.** The disentangling analysis is cheap and nameable: condition each optimizer's gap on how much that optimizer improves over the seed set, using `mean_x0_best` and `mean_ret_best`, which the inversion artifact already stores. Re-analysis, not a new run. **If it holds it unifies six results the paper currently reports separately in five sections** — the interaction eta-squared, the cross-proposal coverage table, the x0-inversion, Elimination 4's budget-widening, the Design-Bench frozen cells (GP+perturbation being the frozen pairing), and the Design-Bench optimizer inversion — and explains the paper's biggest puzzle: why the optimizer axis is negligible on synthetic tasks but inverts on Design-Bench with perturbation leading.

### Rank 3 — Connect the two tables that already measure the same thing. FOLD-INTO-THIS-PAPER / CHEAP.

Supplement `tab:cross` reports premise coverage at beta=2: the ensemble holds at 0.73 in-distribution, **0.41 on its own proposals**, **0.97 on the GP's**; the GP holds at 0.98 / 0.97 / 0.93. The paper's own reading is correct: *"The ensemble is not miscalibrated in general---only where its own gradient optimizer drives it."*

**That sentence is a description of an interaction, and the paper never says so.** The ensemble's premise validity is a property of the ensemble-optimizer *pair*, not of the ensemble. **So the paper has, in two tables, (a) a significant interaction term it never interprets and (b) a direct mechanistic measurement of what that interaction consists of — and it never connects them.** `tab:corners` says the two axes are not separable; `tab:cross` says *why*: each surrogate is well-behaved except where its own search rule pushes it. This upgrades the interaction from a bare eta-squared to a claim with an independent, unnormalized measurement behind it — **coverage figures are probabilities, not min-max-normalized scores**, so the 0.41-versus-0.97 contrast is untouched by the normalizer critique [40]. One sentence each way. Zero computation.

### Rank 4 — The budget axis belongs to a named line the paper does not know it is in. FOLD-INTO-THIS-PAPER / CHEAP. **The clearest under-stated finding.**

The paper presents its budget result **defensively**, as an objection removed: "the obvious objection is that the optimizer axis is search intensity wearing a costume." Read against the literature it is not a defence — it is a contribution to an established line carrying **the strongest evidence in the paper** (the only disjoint intervals anywhere: [0.189,0.355] against [0.421,0.719]) plus a ranking flip.

COCO/BBOB Section 3.5 formally names **budget-dependent benchmarking** as a methodological category with a decade-old apparatus: *"The performance of some solvers depends on the total budget of function evaluations given as an additional parameter to the solver... a single performance assessment of such solvers cannot be expected to faithfully predict their performance for budgets that are different from the one that was used"* — with fixed-budget and fixed-target views and anytime assessment prescribed [50]. COCO is uncited. And a direct precedent sits **already in the paper's bibliography, cited for the genre but not for this**: Lucic et al., verbatim, *"bad models can outperform good models given enough computational budget"* and *"a [model] trained on a large budget can outperform a good model trained on a small budget"* [34]. **That is the paper's own finding — gradient leading at Q=4,352, perturbation at Q=51,456 — stated at NeurIPS 2018.** A sibling-field confirmation exists in metaheuristics, where budget-matched comparison is standard practice and raising the budget "may significantly affect the final verdict."

### Rank 5 — Report the normalizer robustness check and name the fifth coordinate. FOLD-INTO-THIS-PAPER / CHEAP.

Covered in item 14d. Two things make it more than defensive. **The repository already knew**: `docs/FLAW_LEDGER.md` P1-2 flags exactly this, and `docs/scripts_eta_robustness.py` is a prior, abandoned attempt — it reproduces the paper's eta-squared to eight decimal places and recomputes under rank, z-score, log-regret and winsorized normalizations, but every number in its raw output was computed on the **uncorrected published grid**, never on the corrected headline, the four corners, or the sweeps [40]. And the machinery exists twice over, since the per-task z-score cross-check is already used for Elimination 1. **This lands almost exactly where the paper's own framing sits** — "the direction is invariant... only the size of the gap moves" — so the fix extends an argument the paper already makes to one more axis, supportively.

### Rank 6 — Report the computed TOST bound. FOLD-INTO-THIS-PAPER / CHEAP.

The supplement's entire treatment of equivalence is one clause: paired TOST tests "are computed by the released `stats.py`." **They were, and the result is informative:** gap 0.3762, 90% CI [-0.1078, 0.8602], effect bound 0.4840, **not equivalent at either a 0.5 or a 0.3 margin**. The paper is scrupulous about never claiming equivalence and correctly says a failure to reject is not a demonstration of absence — **but it has the actual equivalence test in hand and it is a positive statement about what the null does and does not license**, stronger than the disclaimer in its place. Lakens (2017), the standard reference covering paired-means TOST with Cohen's d_z, is absent from the bibliography, and his caution should travel with the number: "rejecting small effects in an equivalence test requires large samples." A confirmed negative closes the thread cleanly: there is no ML-benchmark-specific equivalence-testing literature — OpenAlex and arXiv return only clinical hits. Zero new computation.

### Rank 7 — The landscape negative, in three sentences. FOLD-INTO-THIS-PAPER / CHEAP.

A decisive quantified negative, more useful than the speculative positive one might expect. Tested against `tab:sfull` under all three optimizers: **no standard landscape covariate predicts the GP-versus-ensemble gap.** Spearman with dimension is 0.107 (p=0.82) under gradient, -0.109 under perturbation, 0.071 under CMA — indistinguishable from zero under every optimizer. Textbook multimodality **actively mis-sorts**: within the four-function "many local minima" bucket the gap spans 2.09 to 2591.06, three orders of magnitude inside one category, while the only strictly unimodal task has the smallest gap of all and Styblinski has the second-largest. Separability fares no better [51].

**But the heterogeneity is not noise**: the per-task gap ranking is **stable across optimizers at Spearman 0.84-0.96**. Composed with rank 2, this is a constraint on any mechanism account that no single analysis produced — tasks keep their rank order under every optimizer while the overall scale collapses under the weak one, ruling out both a purely optimizer-side and a purely task-side story. **The mechanism has to be multiplicative: a task factor scaled by an optimizer-aggressiveness factor.** Reporting this **closes a hostile reviewer's "isn't this just landscape?" objection with a falsifiable, already-computed answer** instead of the current one-line hedge. One documented curiosity is worth a clause: Griewank is known for "getting simpler to be optimized with increasing dimension, although the number of local minima increases," so attributing the ensemble's collapse there to ruggedness is probably backwards [51].

### Rank 8 — Name the inversion, and cite the guarantee it lacks. FOLD-INTO-THIS-PAPER / CHEAP.

The paper states its inversion result flatly: "we call that an inversion... a demonstration within this grid, descriptive at n=7 tasks, not a mechanism." **It is an instance of a named, decades-old failure category.** The offline-RL literature formalizes exactly this as **Safe Policy Improvement**: `P(rho(pi,M) >= rho(pi_b,M) - zeta) >= 1 - delta`, the requirement that a policy learned offline not be worse than the baseline it was given, with high probability. Thomas, Theocharous & Ghavamzadeh (ICML 2015) is the founding formalization; Laroche, Trichelair & Tachet des Combes prove a zeta-approximate guarantee and — pointedly — measure safety via the **1%-CVaR** across ~100,000 randomly generated MDPs, precisely because algorithms lacking such a guarantee return policies below the baseline in a nontrivial fraction of runs [52]. **The reframing:** the inversion is not an awkward number, it is the observation that **offline MBO optimizers ship with no safe-improvement guarantee, and the absence bites.** The sibling field built algorithms around this a decade ago; offline MBO has not. Two sentences, two citations, and a descriptive count becomes a statement about a missing guarantee.

### Rank 9 — Test the one candidate mechanism the seven eliminations do not rule out. FOLD-INTO-THIS-PAPER / CHEAP if per-member predictions were stored; FOLLOW-UP if not.

Ghasemipour, Gu & Nachum prove that **shared pessimistic targets across ensemble members can render an ensemble paradoxically optimistic**, with member independence as the load-bearing condition [27]. Directly on point for a paper whose central puzzle is that a pessimistic, LCB-penalised ensemble ranks its own hallucinations above real data. **If insufficient member independence converts intended pessimism into effective optimism, that is a positive mechanism for the inversion — the first this audit found that the seven eliminations do not already exclude.** Testable on data the paper has: measure pairwise prediction correlation across the K=5 members at the returned optima and check whether inversion rate tracks it. This compounds with item 4: an MSE-trained ensemble using cross-member empirical variance as sigma [26] is precisely the construction most vulnerable to insufficient diversity [29]. **Two independent lines now point at ensemble construction as an unexamined confound, and the paper treats its ensemble as a fixed given.**

### Rank 10 — Xu et al.'s extrapolation theorem: the mechanism the paper is missing. FOLD-INTO-THIS-PAPER / CHEAP.

Xu et al., "How Neural Networks Extrapolate" (ICLR 2021), Theorem 1: in the NTK regime, ReLU MLPs **provably converge to linear functions along rays from the origin outside the training support**, at rate O(1/t). This turns the paper's own diagnosis into a falsifiable geometric prediction: **an ensemble mean growing linearly without bound far from support admits unbounded maximizers at the box boundary; a GP posterior mean, reverting toward its prior, does not.** "Which off-distribution maximizers a surrogate's mean admits" stops being a diagnosis and becomes a testable claim about each surrogate's far field. **Runnable now** — fit lines to existing model outputs along already-stored optimization trajectories, no new training. **A flagged tension resolves: this is compatible with Elimination 2**, because the two are asymptotic in different variables — Xu in far-field *distance from support*, Elimination 2 in *width*, which it explicitly disclaims any asymptotic claim about. Xu's robustness check confirms the linear far-field behaviour holds approximately across depths, widths, learning rates and batch sizes [32]. **One clarifying sentence is required if adopted**, because nothing currently distinguishes "NTK regime" from "the kernel limits are asymptotic" for a reader meeting both in one paper.

### Ranks 11-14 — four cheap fold-ins, stated compactly. All FOLD-INTO-THIS-PAPER / CHEAP.

**11. Cite Fannjiang as the origin of the diagnosis, then state the refinement.** Handled as a liability in item 13; the upside is that the paper can name the founding articulation and then state its own advance: **the off-support story is right in aggregate and insufficient as an explanation of the class gap**, because at matched distance the classes still differ — medians 0.87 / 0.86 / 0.84, with the ensemble worse by -1.406 in true oracle value. That is a genuine advance on a named prior position, and far stronger than presenting the diagnosis as unprecedented.

**12. Fit Gao's overoptimization curve.** Gao, Schulman & Hilton give closed forms with `d := sqrt(KL(pi||pi_init))`: `R_bon(d) = d(alpha - beta*d)`, `R_RL(d) = d(alpha - beta*log(d))`. Degradation is **class-dependent** — the functional form differs by optimization method — but they vary only *scale within one architecture family*. **So Elimination 7 is a refinement of Gao's principle onto a new axis (surrogate architecture class rather than scale) in a new domain, consistent with the scaling law rather than a counterexample.** Two surrogate classes at matched distance with different true loss is exactly what a class-dependent degradation curve predicts, and the paper has 5,040 instrumented optima to fit it on. The three bib defects go with it.

**13. Reframe the PGS falsification as a named-belief-refuted hook.** The optimizer-axis result locally falsifies a premise asserted verbatim by a paper published **at this venue two years earlier** [43]. The draft uses this only as a scope-limiting aside. Reframed as "a claim made at this venue in 2024 is directly measured and falsified by our factorial," it is the genre's actually-accepted shape — Henderson, Musgrave and Ferrari Dacrema all lead with a named belief refuted, not a decomposition offered cold. **That argument form requires no genre familiarity to register as a contribution**, which matters given AAAI's bundled novelty-and-significance criterion and its stated preference for "new territory" [47], with no analogue to the rubric that pre-authorizes rigorously-supported negative results [48]. One further free move: select the evaluation-and-benchmarking topic keyword rather than a Bayesian-learning one, since keyword choice determines the reviewer pool [53].

**14. Give the beta=0 decomposition a lineage.** Fujimoto & Gu's TD3+BC ablation shows RL alone is "insufficient to achieve a high performance" without the behaviour-cloning term, with performance robust across a broad alpha range and degrading only at the extremes [54] — the same base-versus-regularization decomposition. **State the limit precisely** for the theoretical sibling: PEVI decomposes offline-RL suboptimality and proves pessimism minimax-optimal, but contains **no empirical base-versus-conservatism ablation**, so it supports the framing and is not a precedent for the experiment. That distinction is the whole subject of this audit.

### Ranks 15-17 — the expensive, the caveated, and the free

**15. The double-dissociation transplant. FOLLOW-UP-PAPER / EXPENSIVE** (CHEAP-to-MEDIUM confined to one 2D benchmark). Deep Kernel Learning plus a bi-Lipschitz-constrained deterministic-uncertainty model gives the cleanest **causal** test available: an unconstrained GP-with-neural-features should fail like the ensemble; a constrained variant should recover GP-like behaviour. Also expensive: a three-arm graded battery in the style of BCQ, to upgrade the binary inversion into a dose-response curve; and a genuine BBOB/COCO-scale landscape study with standard ELA feature sets, which would inherit the circularity that landscape features computed *through* a surrogate are "more indicative of the surrogate model than the original landscape" [51].

**16. The (alpha, omega)-sensitivity measure. FOLD-INTO-THIS-PAPER / CHEAP, with a venue caveat.** Dao et al.'s definition perturbs the trained surrogate's own **parameters** and measures how far the prediction at the found optimum moves. **None of the seven eliminations touches weight-space fragility** — a genuinely new axis, computable on already-trained checkpoints with zero new training. **Cite as arXiv:2503.04181 (2025), venue unverified**; see the terminal section.

**17. Two free presentation fixes. NOT-WORTH-IT to expand, free to make.** The budget-matched synthetic arm reports 630 cells, implying 10 seeds rather than the 30 given for main results — state the seed count where the number appears. And the beta-sweep endpoint at beta=2 (0.40636, 10 seeds) and the headline on/on corner (0.40455, 30 seeds) are **two independent runs landing 0.0018 apart** — a replication the paper obscures by letting the reader assume they are one number at two precisions. **A paper whose central scalar is challenged as an operating-point artifact should be advertising an independent reproduction of it.**

### Explicitly NOT-WORTH-IT

Adding meta-commentary citing the venue's own review criteria — it reads as reviewer-gaming and the argument is stronger on the merits [53]. Hunting further for a citation that owns LCB paralysis — Yarotsky is the closest and is a scoping obligation, not an owner, and the frozen-cell evidence is stronger unattributed than mis-attributed. Retracting the K-sensitivity result or the seven eliminations — both are real; what needs narrowing is the priority claim on the first and the scope statement on the second.

---

## What I could not verify and why

Grouped by *why* verification failed, because the reasons differ in how much they should discount a finding.

**A. Paywalled or access-blocked primaries.** **Elsayed & Lacor (2014)** — the most N6-shaped title in the audit; every free route exhausted and no verdict rendered from snippets. **Kelley (1935)** and **Olejnik & Algina (2003)** — paywalled, but this dependency was **upgraded to firsthand** via two open-access primaries (Okada 2013 from a Wayback snapshot of the author's self-archived PDF; Liu 2022, CC BY) plus a first-hand bias computation from the paper's own bootstrap artifacts. **Montgomery, Chapter 5** — reached only through the official SAS/JMP companion volume; the load-bearing DOE citations should be the NIST handbook and Box, which are open and were quoted directly [18][46]. **McClelland & Judd (1993)** — verified via OpenAlex metadata and abstract, full text paywalled [46]. **Yu et al. (2016)** on landscape-driven surrogate selection — blocked by an AWS-WAF bot-challenge on IEEE Xplore, a genuine access failure rather than a budget decision; Malan's 2021 survey quotes it [51]. **Rudolph (1994)** on elitism guarantees — 403 everywhere; the Safe-Policy-Improvement framing carries the point instead and is fully verified [52]. **Locatelli (2003)** on Griewank — abstract only [51].

**B. Venue attributions I could not confirm — and two that were my own errors, not the paper's.** This category matters most, because a report whose subject is citation fidelity must not introduce citation defects. **Dao et al., "Boosting Offline Optimizers with Surrogate Sensitivity"** was relayed as "arXiv:2503.04181, ICML 2024." **arXiv v1 is dated 2025-03-06 — chronologically impossible** — and OpenAlex returns no title match; the venue is **withdrawn** and it is cited as arXiv 2025, venue unverified, though the substance stands. **Ghasemipour, Gu & Nachum** was reported by two separate routes as NeurIPS 2022 and as ICML 2022; OpenAlex resolves arXiv:2205.13703 to a preprint with **no conference venue recorded**, and a title search returns no published version [27]. Cited as arXiv 2022, venue unverified — material, because that source is load-bearing twice, at item 9 and rank 9. **Choi et al.** carries no arXiv Comments field, no Journal-ref and no proceedings footer; flagged venue-unverified rather than guessed. Verified clean in the same sweep: Xu (ICLR 2021, arXiv v1 2020 — the same trap class as Li/Rudner/Wilson), Gao (**ICML 2023**, confirming the `gao2022reward` key and page-range defects), Lu (ICLR 2022 Spotlight), Fujimoto & Gu (NeurIPS 2021 Spotlight) [54], Laroche et al. (**ICML 2019**, not 2018) [52], Henderson (AAAI 2018), Benavoli (JMLR 17, 2016) [42], Li/Rudner/Wilson (ICLR 2024) [9], Kim et al. (TMLR 01/2026) [14], Fan et al. (NeurIPS 2024, DOI 10.52202/079017-4151) [7], Schmidt et al. (ICML 2021, from the PMLR copyright footer), Steiner et al. (TMLR 05/2022), Robinson et al. (from the publisher-hosted PMC header), Okada (PDF running header), Yarotsky (Springer citation metadata), Ament et al. (NeurIPS 2023 Spotlight) and Kim & Choi (ECML-PKDD 2020) [22].

**C. Tooling limits.** The vault's fetch path **cannot ingest PDFs in this environment** — every arXiv PDF URL form returns a binary-garbage rejection, traced to a post-fetch junk-detection gate misfiring rather than a missing extraction dependency, since the underlying PDF routine works when called directly. **Consequence:** most vault notes for PDF sources hold only the arXiv abstract page, roughly 700 words; all full-text verification ran via HTML mirrors or external extraction. **The verdicts above do rest on primary full text**, but a later reader cannot re-grep that text from the stored note, so any verdict resting only on a stored note body is weaker than one resting on the external extraction. **Semantic Scholar returned HTTP 429 for the entire session** on both the REST endpoint and the tool interface, across every retry schedule; arXiv and OpenAlex carried all citation chaining including the forward walks [1]. **OpenAlex full-text search is very noisy on this topic** and its null results are weak evidence of absence; the N6 verdict does not rest on them.

**D. Analyses recommended but not run.** **The mixed-effects model with task as a random effect** — the repository's own flaw ledger calls for it, no normalizer robustness check substitutes for it, and **two independent investigators named it as their falsifier**, making it the single most-cited open item in this audit [40]. **The floor-effect disentangling analysis** (rank 2) — the data exists, it was not run. **Bias correction on the interaction across the beta and budget sweeps** — computed for the four corners only. **A second implementation of any artifact computation** — corner values, inversion counts, frozen-cell counts and raw-units gaps were each computed once. **The eta-squared recomputation from RaM's Table 3 has not been independently reimplemented**, and the N6 defence leans on a number this audit computed once [4].

**E. Questions raised without resolving.** **A numeric coincidence, flagged and explicitly not asserted as a finding.** Elimination 3's `0.375` and `roughly 0.48` sit within rounding of the released TOST outputs `gap = 0.3762` and `effect_bound = 0.4840` — but Elimination 3 is described as a synthetic-grid result from the width/RMSE analysis while the TOST figures come from the Design-Bench results database. Either a three-significant-figure coincidence, which is possible, or a transplanted number in a load-bearing paragraph. **Resolvable by the author in one lookup; neither reading is asserted here**, and an audit that has already caught two of its own overstatements should not manufacture a third. **Whether a depth sweep exists unreported** — item 7's force depends on the ensemble being fixed at two hidden layers throughout the width sweep, read from `main.tex:88`; the *absence* of a depth experiment elsewhere in the artifacts was not confirmed [32]. **Whether `supplement.tex` contains duplicate or contradictory NTK framing** — not checked. **Whether the AutoML and kernel-selection literature treats model class and training objective as one factor** — this could attack N6's surviving ground (1); none was found, but the region was searched for crossed designs rather than for that taxonomic question [19].

**F. Scope boundaries not crossed.** Design-Bench's corrected corner was not run through the normalizer sweep — only the synthetic corners and the uncorrected Design-Bench numbers [40]. **The paper's code was verified at the line level for Confounds 1 and 2 only**; the bootstrap implementation, the ANOVA implementation and `stats.py` were not audited, so **every numeric verdict here assumes those are correct, which is an assumption rather than a finding.** And **no experiment was reproduced**: all numeric verification compares the paper's text against its own stored artifacts, so if an artifact is itself wrong, this audit would not detect it.

**G. Corpus-level gaps.** Two identifiers on this pass's reading list did not resolve in the vault; the canonical notes for the same two sources were read in their place and both are cited above [16][28]. And several primaries whose findings are load-bearing here — RaM's full text, Rahaman, Abe, Fannjiang, Gao, Xu, Dao, Okada, Yarotsky, Hutter and Moosbauer — were verified through this session's depth investigations and external extractions rather than through notes on this reading list. Where that is the case the finding is attributed to the investigation record rather than to a source note, and named in prose so the author can re-obtain it directly.


---

## Sources

[1] N6 Extension-Check Synthesis (R4 adversarial re-audit): forward-citation sweep of Hutter 2014, Liang 2021, Moosbauer 2022 and van Rijn & Hutter 2018. vault note: n6-extension-check-synthesis-r4-adversarial (this audit session)
[2] Kudela & Dobrovsky. "Performance Comparison of Surrogate-Assisted Evolutionary Algorithms on Computational Fluid Dynamics Problems." arXiv:2402.16455, 2024. https://arxiv.org/html/2402.16455
[3] Park, Cheon, Wi & Koh. "BOOST: A Data-Driven Framework for the Automated Joint Selection of Kernel and Acquisition Functions in Bayesian Optimization." arXiv:2508.02332v4. https://arxiv.org/html/2508.02332v4
[4] Interim investigation: N6 residual scoping, legitimate or gerrymandered (RaM Table 3 and Appendix E.5; eta-squared recomputed from RaM published numbers). vault note: interim-report-n6-residual-scoping-legitimate-or-gerrymandered (this audit session)
[5] Demsar, J. "Statistical Comparisons of Classifiers over Multiple Data Sets." JMLR 7(1):1-30, 2006 (full 30-page PDF extracted and grepped). https://jmlr.org/papers/v7/demsar06a.html
[6] Demsar, J. "Statistical Comparisons of Classifiers over Multiple Data Sets." JMLR 7(1):1-30, 2006 (publisher landing page; recommendation text). https://www.jmlr.org/papers/v7/demsar06a.html
[7] Fan, Wang, Ng & Hu. "Minimizing UCB: a Better Local Search Strategy in Local Bayesian Optimization." NeurIPS 2024, arXiv:2405.15285. https://arxiv.org/abs/2405.15285
[8] Liu, Lin, Padhy, Tran, Bedrax-Weiss & Lakshminarayanan. "Simple and Principled Uncertainty Estimation with Deterministic Deep Learning via Distance Awareness" (SNGP). NeurIPS 2020, arXiv:2006.10108. https://arxiv.org/abs/2006.10108
[9] Li, Rudner & Wilson. "A Study of Bayesian Neural Network Surrogates for Bayesian Optimization." ICLR 2024, arXiv:2305.20028. https://arxiv.org/abs/2305.20028
[10] Yang, Yuan, Sun et al. "Support-Proximity Augmented Diffusion Estimation for Offline Black-Box Optimization" (SPADE). ICML 2026, arXiv:2605.11246. https://arxiv.org/abs/2605.11246
[11] Yuan, Chen, Sun, Zhang, Pal & Liu. "Diffusion Large Language Models for Black-Box Optimization." arXiv:2601.14446, 2026. https://arxiv.org/abs/2601.14446
[12] Lyu, Tan, Xue, He, Huang, Zhang & Qian. "On the Learnability of Offline Model-Based Optimization: A Ranking Perspective." arXiv:2603.04000, 2026. https://arxiv.org/abs/2603.04000
[13] Qian, Zhu, Shu et al. SOO-Bench: Benchmarks for Evaluating the Stability of Offline Black-Box Optimization. ICLR 2025 (repository plus full-text verification). https://github.com/zhuyiyi-123/SOO-Bench
[14] Kim, Gu, Yuan, Yun, Liu, Bengio & Chen. "Offline Model-Based Optimization: Comprehensive Review." TMLR 01/2026, arXiv:2503.17286. https://arxiv.org/abs/2503.17286
[15] Liang, Gongora, Ren, Tiihonen et al. "Benchmarking the performance of Bayesian optimization across multiple experimental materials science domains." npj Comput. Mater. 7:188, 2021 (independent re-fetch, full HTML). https://www.nature.com/articles/s41524-021-00656-9
[16] Liang et al., npj Computational Materials 7:188, 2021 (claim-level analysis of the crossed surrogate x acquisition grid). https://doi.org/10.1038/s41524-021-00656-9
[17] Sun, Chen, Yuan, Wu, Gu, Pal & Liu. "Training Diffusion Language Models for Black-Box Optimization" (DiBO). ICML 2026 Spotlight, arXiv:2603.17919. https://arxiv.org/abs/2603.17919
[18] NIST/SEMATECH e-Handbook of Statistical Methods, Sec. 5.3.3.4.3, "Confounding (also called aliasing)". https://www.itl.nist.gov/div898/handbook/pri/section3/pri3343.htm
[19] Abdar et al. "A Review of Uncertainty Quantification in Deep Learning: Techniques, Applications and Challenges." Information Fusion, 2021, arXiv:2011.06225. https://arxiv.org/pdf/2011.06225
[20] Agarwal, Schwarzer, Castro, Courville & Bellemare. "Deep Reinforcement Learning at the Edge of the Statistical Precipice." NeurIPS 2021 Outstanding Paper, arXiv:2108.13264 (full text). https://ar5iv.labs.arxiv.org/html/2108.13264
[21] Mueller, von Rohr & Trimpe. "Local policy search with Bayesian optimization" (GIBO). NeurIPS 2021, arXiv:2106.11899. https://arxiv.org/abs/2106.11899
[22] Kim & Choi. "On Local Optimizers of Acquisition Functions in Bayesian Optimization." ECML-PKDD 2020, arXiv:1901.08350. https://arxiv.org/abs/1901.08350
[23] Padhy, Nado, Ren, Liu, Snoek & Lakshminarayanan. "Revisiting One-vs-All Classifiers for Predictive Uncertainty and Out-of-Distribution Detection in Neural Networks." arXiv:2007.05134. https://arxiv.org/abs/2007.05134
[24] Carrete, Montes-Campos, Wanzenboeck, Heid & Madsen. "Deep Ensembles vs. Committees for Uncertainty Estimation in Neural-Network Force Fields." J. Chem. Phys. 158:204801, 2023, arXiv:2302.08805. https://arxiv.org/abs/2302.08805
[25] Stevens, Prabhushankar & AlRegib. "RADMI: Latent Information Aggregation as a Proxy for Model Uncertainty." IEEE ICIP 2026, arXiv:2605.01502. https://arxiv.org/abs/2605.01502
[26] Lakshminarayanan, Pritzel & Blundell. "Simple and Scalable Predictive Uncertainty Estimation using Deep Ensembles." NeurIPS 2017, arXiv:1612.01474. https://arxiv.org/abs/1612.01474
[27] Ghasemipour, Gu & Nachum. "Why So Pessimistic? Estimating Uncertainties for Offline RL through Ensembles, and Why Their Independence Matters." arXiv:2205.13703, 2022 (venue unverified). https://ar5iv.labs.arxiv.org/html/2205.13703
[28] Ovadia, Fertig, Ren, Nado, Sculley, Nowozin, Dillon, Lakshminarayanan & Snoek. "Can You Trust Your Model's Uncertainty? Evaluating Predictive Uncertainty Under Dataset Shift." NeurIPS 2019, arXiv:1906.02530. https://arxiv.org/abs/1906.02530
[29] D'Angelo & Fortuin. "Repulsive Deep Ensembles are Bayesian." NeurIPS 2021, arXiv:2106.11642. https://ar5iv.labs.arxiv.org/html/2106.11642
[30] Fort, Hu & Lakshminarayanan. "Deep Ensembles: A Loss Landscape Perspective." arXiv:1912.02757. https://arxiv.org/abs/1912.02757
[31] Jacot, Gabriel & Hongler. "Neural Tangent Kernel: Convergence and Generalization in Neural Networks." NeurIPS 2018, arXiv:1806.07572 (fresh re-fetch). https://ar5iv.labs.arxiv.org/html/1806.07572
[32] Interim investigation: the NTK / width / spectral-bias citations behind Elimination 2 (Jacot; Lee 2018; Lee 2019; Rahaman 2019; Xu 2021). vault note: interim-report-ntk-width-citations-never-verified (this audit session)
[33] Melis, Dyer & Blunsom. "On the State of the Art of Evaluation in Neural Language Models." ICLR 2018, arXiv:1707.05589. https://arxiv.org/abs/1707.05589
[34] Lucic, Kurach, Michalski, Bousquet & Gelly. "Are GANs Created Equal? A Large-Scale Study." NeurIPS 2018, arXiv:1711.10337 (full text). https://ar5iv.labs.arxiv.org/html/1711.10337
[35] Dewolf, De Baets & Waegeman. "Valid prediction intervals for regression problems." Artificial Intelligence Review 55:577-613, 2022, arXiv:2107.00363. https://arxiv.org/html/2107.00363
[36] Hamdan, Love, von Polier, Weis, Schwender, Eickhoff & Patil. "Confound-leakage: Confound Removal in Machine Learning Leads to Leakage." arXiv:2210.09232, 2022. https://arxiv.org/abs/2210.09232
[37] Tibshirani, Barber, Candes & Ramdas. "Conformal Prediction Under Covariate Shift." NeurIPS 2019, arXiv:1904.06019 (full text). https://ar5iv.labs.arxiv.org/html/1904.06019
[38] Tibshirani, Barber, Candes & Ramdas. "Conformal Prediction Under Covariate Shift." arXiv:1904.06019 (abstract page; known-versus-estimated weight wording). https://arxiv.org/abs/1904.06019
[39] Angelopoulos & Bates. "A Gentle Introduction to Conformal Prediction and Distribution-Free Uncertainty Quantification." arXiv:2107.07511. https://arxiv.org/html/2107.07511
[40] Interim investigation: min-max normalization outlier fragility (33-combination normalizer recomputation on the paper's own raw artifacts; Jordan 2020; Bellemare 2013). vault note: interim-report-minmax-normalization-outlier-fragility (this audit session)
[41] Jordan, Chandak, Cohen, Zhang & Thomas. "Evaluating the Performance of Reinforcement Learning Algorithms." ICML 2020, arXiv:2006.16958. https://ar5iv.labs.arxiv.org/html/2006.16958
[42] Benavoli, Corani & Mangili. "Should we really use post-hoc tests based on mean-ranks?" JMLR 17, 2016 (arXiv:1505.02288, 2015). https://arxiv.org/abs/1505.02288
[43] Chemingui, Deshwal, Hoang & Doppa. "Offline Model-Based Optimization via Policy-Guided Gradient Search" (PG-GS). AAAI 2024, arXiv:2405.05349. https://arxiv.org/html/2405.05349v1
[44] Xu, Hutter, Hoos & Leyton-Brown. "SATzilla: Portfolio-based Algorithm Selection for SAT." JAIR 32:565-606, 2008. https://jair.org/index.php/jair/article/download/10556/25269
[45] Alissa, Sim & Hart. "Automated Algorithm Selection: from Feature-Based to Feature-Free Approaches." Journal of Heuristics 29:1-38, 2023. https://link.springer.com/content/pdf/10.1007/s10732-022-09505-4.pdf
[46] Interim investigation: the buried interaction term and its methodological grounding (formal simple-effects table from stored per-seed data; NIST and Box DOE authority; McClelland & Judd). vault note: interim-report-interaction-term-buried-and-its-methodological-grounding (this audit session)
[47] AAAI-27 Main Technical Track Call for Papers. https://aaai.org/conference/aaai/aaai-27/main-technical-track-call/
[48] NeurIPS Evaluations and Datasets 2026 Reviewing Guidelines. https://neurips.cc/Conferences/2026/EvaluationsDatasetsReviewerGuidelines
[49] Sommet, Weissman, Cheutin & Elliot. "How Many Participants Do I Need to Test an Interaction?" Advances in Methods and Practices in Psychological Science, 2023. https://journals.sagepub.com/doi/10.1177/25152459231178728
[50] Hansen, Auger, Ros, Mersmann, Tusar & Brockhoff. "COCO: A Platform for Comparing Continuous Optimizers in a Black-Box Setting." arXiv:1603.08785 (full text; Sec. 3.5, budget-dependent benchmarking). https://ar5iv.labs.arxiv.org/html/1603.08785
[51] Interim investigation: does landscape structure predict which surrogate wins (per-task gap correlations against dimension, modality and separability; ELA and algorithm-selection literature). vault note: interim-report-landscape-predicts-which-surrogate-wins (this audit session)
[52] Laroche, Trichelair & Tachet des Combes. "Safe Policy Improvement with Baseline Bootstrapping" (SPIBB). ICML 2019, arXiv:1712.06924. https://ar5iv.labs.arxiv.org/html/1712.06924
[53] Interim investigation: AAAI venue fit for the audit genre (AAAI-27 review criteria against the NeurIPS Evaluations & Datasets rubric; genre-instance venue confirmations). vault note: interim-report-aaai-venue-fit-for-the-audit-genre (this audit session)
[54] Fujimoto & Gu. "A Minimalist Approach to Offline Reinforcement Learning" (TD3+BC). NeurIPS 2021 Spotlight, arXiv:2106.06860. https://ar5iv.labs.arxiv.org/html/2106.06860
